# Same contract as the root .npmrc: engines (^24.19.0) enforced as a gate.
# Worker installs run from THIS directory (`cd worker && npm i`) — never
# `npm --prefix worker install` from the root (file:.. self-dep bug).
engine-strict=true
