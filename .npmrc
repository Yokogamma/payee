# Makes the engines contract in package.json a GATE, not a warning: npm ci /
# npm i fail on a Node outside `^24.19.0` instead of printing EBADENGINE into
# a log nobody reads. CI installs the version from .nvmrc; locally use
# `nvm use` / your version manager.
engine-strict=true
