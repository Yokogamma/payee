# Matamata Notes — план ребрендинга и запуска через GitHub

Статус: черновик для обсуждения. Пункт 2 общей стратегии (первые бесплатные
пользователи через GitHub). Ничего из этого ещё не реализовано.

## Легенда бренда

**Matamata Notes** — заметки, которые живут вечно, как черепаха матамата
(Chelus fimbriata). Черепаха — символ долголетия и неуязвимости (панцирь =
шифрование), матамата — мастер маскировки (заметки неотличимы от шума без
ключа). Слово «matamata» само по себе зеркально: **mata|mata** — две
одинаковые половины, как две петли знака бесконечности ∞. Это и есть ядро
визуальной идеи: черепаха + ∞.

Проверка имени (2026-08): конфликтов в категории заметок не найдено.
Существующие «Matamata» — город в Новой Зеландии (Хоббитон), бразильское
футбольное приложение «Mata-Mata» в Google Play, приложение библиотеки
Matamata-Piako. Другие категории, риск низкий. Товарный знак не проверялся —
отдельная задача, если дойдёт до регистрации.

## Техническая карта переименования (что можно трогать, а что нельзя)

> **Решение (2026-08-19):** все существующие заметки — тестовые, поэтому
> выполнен ПОЛНЫЙ ребрендинг всех трёх слоёв: тег `App-Name: MatamataNotes`,
> соль KDF `matamata-notes-v1`, storage-ключи `matamata-notes-*`, воркер
> `matamata-notes-proxy`. Старые тестовые заметки и локальные сессии стали
> недоступны — ожидаемо и принято. С первого реального пользователя таблица
> ниже снова становится законом: нижние два слоя больше не трогать.

Аудит кода показал три слоя, у каждого свои правила:

| Слой | Примеры | Можно менять? |
|---|---|---|
| Бренд (видит пользователь) | `<h1>Eternal Notes</h1>` (Landing, Onboarding, PinUnlock, AppNav), manifest `name`/`short_name`, `index.html` title, README, иконки | Да, свободно |
| On-chain идентификатор | `APP_NAME = 'EternalNotes'` → тег `App-Name` Arweave-транзакций (`src/lib/arweave.ts:39`) | Осторожно: restore ищет заметки по этому тегу. Варианты: (а) оставить как есть — тег внутренний, пользователь его не видит; (б) новые загрузки помечать `MatamataNotes`, а restore запрашивать оба тега. Рекомендация: вариант (а) на старте, (б) — если тег когда-нибудь станет публичной витриной |
| Никогда не менять | Соль KDF `'eternal-notes-v1'` (`src/lib/crypto.ts`), ключи localStorage/IndexedDB (`eternal-notes-session`, `eternal-notes-draft`, БД `eternal-notes`, BroadcastChannel `eternal-notes-vault`) | Нет. Смена соли = все существующие заметки нечитаемы. Смена storage-ключей = потеря локальных сессий без миграции. Внутренние имена ≠ бренд |

## Задачи

### A. Бренд и логотип

- [ ] A1. Выбрать концепцию логотипа (см. три варианта ниже) — решение владельца
- [ ] A2. Отрисовать SVG-мастер логотипа + фавикон
- [ ] A3. Сгенерировать иконки: `icon-192.png`, `icon-512.png`,
      `icon-512-maskable.png` (глиф в safe-zone), `apple-touch-icon.png`,
      favicon. Палитру оставить текущую тёмную (#191612) или обновить — решить в A1
- [ ] A4. Мини-брендбук: цвета, шрифт wordmark, правила использования (1 страница в docs/)

Концепции «черепаха + ∞»:

1. **Панцирь-бесконечность** — вид сверху: панцирь матаматы стилизован как две
   петли ∞ с характерной бугристой фактурой. Читается и как черепаха, и как ∞.
2. **Шея-петля** — силуэт сбоку: длинная змеиная шея матаматы (её фирменная
   черта) изгибается назад и замыкается в петлю ∞.
3. **Минимализм** — чистый знак ∞, где одна петля завершается треугольной
   плоской головой матаматы. Лучше всего масштабируется в фавикон 16×16.

Wordmark-идея: MATA∞MATA или mata·mata — зеркальность слова обыграть
симметрией знака.

### B. Переименование в коде и инфраструктуре

- [x] B1. UI-строки: Landing, Onboarding, PinUnlock, AppNav → «Matamata Notes»
- [x] B2. Manifest (vite.config.ts): `name: "Matamata Notes"`,
      `short_name: "Matamata"`; `lang` — см. вопрос Q3
- [x] B3. `index.html` title
- [x] B4. README: заголовок, легенда бренда, ссылка на notes.matamata.dev
- [x] B5. `package.json` name → `matamata-notes` (+ worker `matamata-notes-proxy`,
      лок-файлы перегенерированы)
- [x] B6. Комментарии-шапки модулей обновлены (docs/ROLLBACK.md не тронут —
      там исторические цитаты опубликованных релиз-нотов)
- [ ] B7. Переименовать GitHub-репозиторий `payee` → `matamata-notes` —
      **действие владельца в Settings репозитория** (GitHub ставит редиректы
      автоматически). После переименования поправить дефолт
      `VITE_BASE=/matamata-notes/` в vite.config.ts
- [ ] B8. Передеплой воркера: `wrangler.toml` теперь `matamata-notes-proxy` —
      деплой создаст НОВОГО воркера с чистым состоянием DO (инвайты, allowlist,
      квоты обнулятся) и новым `*.workers.dev` origin. Чек-лист: задеплоить →
      перевыпустить инвайты → обновить `VITE_PROXY_URL` → пересобрать клиент с
      `VITE_TRUSTED_OWNERS` → удалить старого воркера `eternal-notes-proxy`
- [x] B9. Проверка после переименования: клиент 1052 теста + lint + typecheck,
      воркер 109 + 21 тестов + typecheck — всё зелёное

### C. Витрина репозитория

- [ ] C1. README.md на английском: одна крылатая фраза («Notes that outlive
      you. Encrypted. Forever.» — черновик), GIF-демо, ссылка на живое
      приложение, схема «как это работает» (seed → E2E → Arweave), FAQ
- [ ] C2. README.ru.md — текущий русский README, взаимные ссылки
- [ ] C3. GitHub topics: `pwa`, `e2e-encryption`, `arweave`, `notes`,
      `local-first`, `privacy`, `offline-first`, `permaweb`
- [ ] C4. SECURITY.md: threat model кратко + канал для repорта уязвимостей
- [ ] C5. CONTRIBUTING.md + разметка 3–5 issues метками `good first issue` /
      `help wanted`; ревизия 15 открытых issues (закрыть неактуальные)
- [ ] C6. Записать GIF/видео демо (открыл → написал → закрыл → восстановил по
      seed на «другом устройстве»)
- [ ] C7. GitHub Release v0.x с changelog — сигнал живого проекта
- [ ] C8. Соц-превью репозитория (og-image 1280×640 с логотипом)
- [ ] C9. About-блок репо: описание + ссылка на живое приложение

### D. Снятие инвайт-барьера (без пользователей звёзды бесполезны)

- [ ] D1. Решить модель доступа для первых пользователей (см. Q4):
      demo-режим / форма запроса инвайта / автоинвайт с квотой
- [ ] D2. Если demo-режим: локальные заметки без загрузки в Arweave
      («попробовать без сохранения навечно»), баннер с CTA на инвайт
- [ ] D3. Инструкция self-host для Worker (свой кошелёк, свои инвайты) —
      открывает дорогу в awesome-selfhosted и снимает вопрос «а кто платит»

### E. Дистрибуция (после C и D, порядок важен)

- [ ] E1. PR в awesome-списки: awesome-arweave, awesome-privacy,
      awesome-selfhosted (требует D3), arweave ecosystem каталоги
- [ ] E2. Show HN (текст поста: техническая история — E2E + permaweb +
      state-machine синка; HN любит криптодизайн без токенов)
- [ ] E3. Product Hunt запуск (нужны: логотип, галерея, tagline — из A и C)
- [ ] E4. Reddit: r/privacy, r/selfhosted, r/DataHoarder, r/Arweave;
      local-first сообщество (localfirst.fm, Discord)
- [ ] E5. Пост в Arweave/permaweb сообществах (их каталоги дают целевой трафик)
- [ ] E6. Dev-статья «как мы строили вечные заметки» (Habr — русская аудитория,
      dev.to — англоязычная)

### F. Метрики успеха этапа

- [ ] F1. Определить цели: например, 100 звёзд / 50 установок PWA / 12+
      активных пользователей (⇒ одновременно закрывает тестеров для Google Play
      из пункта 1 общей стратегии)
- [ ] F2. Простая аналитика без слежки (Cloudflare Web Analytics — cookieless)

## Вопросы к владельцу (блокируют соответствующие задачи)

- **Q1 — ЗАКРЫТ (2026-08-19).** Работаем на `notes.matamata.dev`; владелец
  дополнительно регистрирует `matamata.app` (защита бренда + основной домен
  для Google Play в будущем).
- **Q2 (блокирует B7).** Подтверждаешь переименование репозитория
  `Yokogamma/payee` → `Yokogamma/matamata-notes`? Старые ссылки продолжат
  работать через редирект. Само переименование — кнопка в Settings
  репозитория (только владелец).
- **Q3 (блокирует B2, C1, D2).** Язык продукта: UI сейчас полностью русский.
  Для глобального запуска (HN, PH, awesome-списки) нужен английский UI.
  Делаем i18n (ru+en) до запуска или запускаемся на русскую аудиторию, а
  английский — фаза 2?
- **Q4 (блокирует D1).** Модель доступа: кто и как получает инвайт? Готов ли
  сервер оплачивать AR за N бесплатных пользователей, какая квота на
  пользователя? Или demo-режим без Arweave + инвайт по запросу?
- **Q5 (блокирует A2).** Логотип: выбери концепцию 1/2/3 (или комбинацию) — я
  могу отрисовать SVG-варианты на утверждение; либо заказываешь у дизайнера,
  тогда A1–A4 — только ТЗ.
- **Q6 (блокирует E2, E3).** От чьего имени запускаемся: есть ли аккаунты
  Product Hunt / HN / X (Twitter)? Запуск от личного аккаунта работает лучше,
  чем от свежесозданного проектного.
- **Q7 — ЗАКРЫТ (2026-08-19).** Данные тестовые → полный ребрендинг:
  `App-Name: MatamataNotes`, соль `matamata-notes-v1`, все storage-ключи
  переименованы. Обратной совместимости со старыми тестовыми заметками нет.

## Порядок исполнения

1. Ответы на Q1–Q5 → A (логотип) и B (переименование) параллельно
2. C (витрина) — сразу после B, не ждёт логотипа кроме C8
3. D (доступ) — параллельно с C
4. E (дистрибуция) — только когда A–D готовы: первое впечатление не переиграть
5. F — до начала E, чтобы было с чем сравнивать
