# План: устойчивость доступа к Arweave (multi-gateway, метрики, restore.html)

Статус: **v17 — закрытие ревью по v16 (1 high + 1 medium + 1 low,
2026-08-22)**:

1. **HIGH — `activate` привязан к конкретной резервации и её reward.**
   В v16 разрешённая пере-резервация до подписания сочеталась с
   `activate(spendKey)` без параметров — гонка «резерв 100 → конкурентный
   `prepare` на 1 → подпись на 100 → активация учёта на 1» занижала
   `pendingReservations` и обходила cap/floor. Нормативно: `prepare`
   возвращает монотонный `revision`; durable `signed` несёт
   `{spendKey, reward, spendRevision}`; `activate(spendKey, reward,
   revision)` — атомарный CAS; несовпадение обрабатывается той же ветвью,
   что истёкший lease (пере-резервация на reward ИЗ `signed` под всеми
   проверками `prepare`). Обязательный тест гонки — §4.PR-3b.
2. **MEDIUM — граница зависших резервов исправлена.**
   `MAX_RECOVERY_INFLIGHT × MAX_TX_REWARD_WINSTON` — граница НА ОДИН ключ
   (cap живёт в per-key DO), для N ключей она умножается на N; фактическую
   глобальную границу задают сами пределы `SpendGuard`
   (`pendingReservations` входит в проверки `prepare`). Следствие описано
   явно как **fail-closed outage**: `active` без TTL может бессрочно занять
   весь бюджет до reconciliation; многопользовательский тест добавлен.
3. **LOW:** §6 п.1 ссылался на «план v15» — заменено ссылкой на актуальную
   ревизию шапки, чтобы merge gate не указывал на разные редакции.

Предыдущий шаг: **v16 — сага D10, детерминированный бюджет, формула D11 по
operatorId, реализация PR-2 (2026-08-22)**. Содержание v16:

1. **HIGH ревью 13:** «резервация освобождается в той же транзакции, где
   запись переходит в `redrop_pending`» было технически невозможно —
   `SpendGuard` — глобальный DO, `RateLimiter` — per-key
   (`index.ts:670`), одна `storage.transaction()` два объекта не
   охватывает. Координация переписана на **идемпотентную сагу**
   `prepared → active → settled` с ключом `spendKey` — §4.PR-3b
   «Предохранитель D10», включая нормативную монотонность `settle`
   (H1 ревью 15) и бакет часа ФАКТИЧЕСКОГО settle (M ревью 16).
2. **Детерминированный скользящий бюджет:** окно 24 ч по часовым бакетам,
   хранится до 25 бакетов (M3 ревью 14), учёт в момент `settle('spent')`.
3. **Формула D11 уточнена:** отсутствие txId утверждается только между
   источниками с `complete = true`; независимость голосов кворума — по
   пиненной карте `canonicalOrigin → operatorId`, fail-closed (M4 ревью 14).
4. **Все численные дефолты D10/D11 — ПРЕДВАРИТЕЛЬНЫЕ** (M5 ревью 14):
   финальные значения утверждаются по shadow-метрикам PR-3a/PR-4.
5. Вычищены пережившие v15 противоречия: §7 реш. 5 и строка PR-4 в §8
   (безусловный «metadata-конфликт → incomplete»), тест «медианы баланса»
   в списке тестов D10.
6. В §4.PR-2 добавлен подраздел **«Реализация (2026-08-21)»** — полная
   спецификация единственного незаблокированного кодового шага (ревью
   14–24 плана-спутника), с актуальными якорями кода.

**Консолидация (решение владельца, 2026-08-21): документ теперь ОДИН — этот
файл.** Локальный план-спутник упразднён (сокращён до указателя сюда),
замороженные ревью-ветки не используются. Единственная ветка документа —
`docs/arweave-plan-v15` (имя — исторический ярлык), PR #87 — открытый
«катящийся» ревью-PR: каждая новая редакция — коммит в него, в `main`
мержится на стабильных вехах. Единственная ссылка для ревью:
`https://github.com/Yokogamma/payee/blob/docs/arweave-plan-v15/docs/ARWEAVE-RESILIENCE-PLAN.md`.
Гейт кода (M ревью 15): АКТУАЛЬНАЯ ревизия отревьюена → PR #87 смержен →
ветка PR-2 создаётся от СВЕЖЕГО `main` — код не начинается ни от `main` без
влитых редакций, ни в документационной ветке.

Предыдущий шаг: **v15 — доработка протоколов по ревью 12** (2 high + 2 medium:
tenant в ключе `SpendGuard`; баланс — консервативный минимум вместо медианы
по двум ответам, цена — медиана по ≥3 либо точное совпадение двух; union
больше НЕ удаляет txId по расхождению неподписанных тегов (арбитр — header
D9); `block = null` полностью исключён из решения D11, порог задан в
подтверждениях). Предыдущий шаг: **v14 — протоколы к D9–D11 по ревью 11** (2 high + 1 medium
+ 1 low: полный входной контракт верификации `txId ↔ bytes` со сверкой
доверенного владельца; денежный и атомарный контракт глобального
предохранителя в Winston; доказуемый источник возраста для D11 из кворума
статусов; ADR расширен до D1–D11). До отдельного раунда ревью этих
протоколов PR-3a, writer-часть PR-3b и PR-4 НЕ стартуют — PR-2 не
заблокирован (PR-1 смержен в main 2026-08-20). Предыдущий шаг: **v13 — интегрирована security-ревизия контура**
(`C:\Users\user\.claude\plans\arweave-gateway-security.md`, 2026-08-20:
5 находок F1–F5 / 5 рекомендаций R1–R5). Тот документ смотрит на тот же
контур с другой стороны — «шлюз ЛЖЁТ» вместо «шлюз ЛЕЖИТ», — и три его вывода
меняют условия выпуска PR-ов ЭТОГО плана, поэтому внесены сюда нормативно
(D9–D11, §2), остальное разобрано в §2.2 и §3. Ключевое: пул payload-шлюзов
без верификации `txId ↔ bytes` делает integrity ХУЖЕ, а не лучше (гейт
PR-3a), и автономный redrop из PR-3b нельзя выпускать без глобального
предохранителя кошелька. История: v12 — механические правки
согласованности (ревью 10 по v11: 1 medium + 2 gap — приняты: alarm-тест
переформулирован «вызов signer — только в фазе 2; durable-коммит и POST —
только CAS-победителю»; ADR-инвариант same-signed-tx = «не создаёт второй
durable/postable txId без доказанного dead»; cap сформулирован через
recovery-записи/durable txId). История: v11 — инвариант generation
переформулирован с «одна ПОДПИСЬ» на «один DURABLE/POSTABLE txId»,
эфемерные подписи отбрасываются, `resign_violation` считает durable-txId,
`deadTxId`-lineage в схеме (ревью 9); v10 — alarm-подпись только в фазе 2, durable-источник фазы 2 =
сохранённый `signedTx` (ревью 8); v9 — durable `redrop_pending`
scheduler-visible и в общем cap (`recoveryCount`), reader-floor возобновляет
фазу 2 (ревью 7); v8 —
`storage.transaction` как ЕДИНСТВЕННЫЙ механизм атомарности scheduler-мутаций
и гарантированное alarm-reschedule при исключениях (ревью 6); v7 —
single-alarm scheduler + CAS, verdict-таблица, программный hash-барьер, лимит
SQLite-DO; v6 — формула dead-кворума, bounded `signed`, логические индексы,
cold-flow, CORS `/raw`; v5 — решения владельца + живые пробы, D7 утверждён
(§2.1); v4 — три раунда ревью (ревью 1: 2 critical + 6 high + 4 medium;
ревью 2: 8; ревью 3: 3). Дата: 2026-08-22. Строится поверх задеплоенного
инкрементального sweep (PR #53). Принцип: каждое спорное место — ОДНО
нормативное решение без альтернатив.

Документ адресован ревьюеру: фиксирует решения, объём, разбиение на PR,
инварианты, тестовые сценарии и границы. Ключевое изменение v2: **PR-3
разделён на read-path и write-path**, потому что write-path (upload failover)
несёт риск второй платной транзакции и требует более строгого протокола, чем
read-path.

---

## 1. Контекст: проверенные факты

Постоянное хранение (Arweave) и постоянный доступ (gateway) — разные свойства.
Сегодня доступ жёстко завязан на единственный `arweave.net`:

| Место | Файл | Что делает |
|---|---|---|
| Клиент | `src/lib/arweave.ts:207` | проба доступности `/info` |
| Клиент | `src/lib/arweave.ts:242` | статус транзакции `/tx/{id}/status` |
| Клиент | `src/lib/arweave.ts:513` | GraphQL-поиск при restore |
| Клиент | `src/lib/arweave.ts:718` | payload `/raw/{txId}` |
| Worker | `worker/src/index.ts:157, 779` | `Arweave.init` (anchor+price+post; якоря v16 по `origin/main` `95be581`) |
| Worker | `worker/src/index.ts:981` | серверная перепроверка статуса |
| CSP | `scripts/postbuild.mjs:39` | `connect-src` пинит только `arweave.net` + proxy |

Дефекты семантики статусов: клиент трактует одиночный `404` → `dropped`
(`arweave.ts:247`); Worker — `404` **и `400`** → `dead` (`index.ts:988`).

Что УЖЕ защищено кодом (проверено, менять не нужно):

- подмена payload между версиями невозможна: внутренний `id` конверта обязан
  совпадать с Note-Id-тегом (`arweave.ts:795–803`), каждая версия — свой uuid;
- «текущая» версия — по аутентифицированному timestamp ИЗНУТРИ AES-GCM-конверта
  (`chains.ts`, `byCurrentness`), не по данным шлюза;
- AES-GCM — фактический аутентификатор кандидатов: чужой/поддельный payload не
  расшифровывается и молча пропускается; обе половины сейфа обязаны
  расшифроваться (fail-closed), claim Note-Id — строго ПОСЛЕ decrypt.

**Точная формулировка про `TRUSTED_OWNERS` (исправлено в v13 по security-ревизии,
F1).** Ранее здесь стояло «restore fail-closed по `VITE_TRUSTED_OWNERS`» — это
верно лишь наполовину и вводило в заблуждение ровно там, где план добавляет
новые индексы. Факты по коду:

- `assertTrustedOwners()` (`arweave.ts:628`) проверяет, что список НАСТРОЕН, —
  это защита от пустого пина, а не проверка владельца каждой транзакции;
- фильтр `owners: $owners` в GraphQL исполняет **сам шлюз**; клиент выбирает
  `node { id, tags {...} }` **без** `owner.address` (`arweave.ts:487–504`) и
  нигде не сверяет фактического владельца ребра (`arweave.ts:662–689`);
- комментарий `arweave.ts:484–486` («атакующий не может опубликовать под этими
  адресами, поэтому подделанные Note-Id не появятся») **переоценивает**
  гарантию: она держится на честности шлюза.

Реальная клиентская защита сегодня — decrypt + inner-id, а НЕ owners-фильтр.
Для одного шлюза разницы нет (подделка не расшифруется), но с приходом
`INDEX_SOURCES` (PR-4) недобросовестный индекс перестаёт быть гипотезой.
Отсюда D9 (§2): смысловая проверка владельца существует только как часть
верификации подписи транзакции; добавление поля `owner.address` в выборку
GraphQL само по себе НЕ решение — это поле возвращает тот же шлюз и он волен
указать в нём доверенный адрес. Комментарий в коде правится вместе с PR-3a.

Инкрементальный sweep (PR #53) уже принял модель недоверия одному индексу:
сознательно НЕТ block-height cursor (курсор навсегда терял бы отставшую
транзакцию). Multi-index — прямое продолжение той же логики.

**Граница защиты (уточнение по ревью).** Инварианты выше и multi-index
защищают от подмены и от умалчивания ОДНОГО индекса. Они НЕ доказывают
глобальную полноту: все настроенные индексы могут отставать или согласованно
умалчивать. `incomplete=false` означает строго «все настроенные ЛОГИЧЕСКИЕ
источники дочитаны до конца» (v6: через какой-либо свой transport, §4.PR-4),
а не «на chain больше ничего нет». Остаточная Byzantine-модель фиксируется в
ADR и, где уместно, в UI.

## 2. Зафиксированные решения

- **D1. Список шлюзов — build-time**, как `VITE_TRUSTED_OWNERS`; смена = релиз.
- **D2. Multi-index restore — всегда параллельно.** Осознанная цена: метаданные
  активности (Owner-Hash, число транзакций, IP) видят все операторы шлюзов.
  Формулировка эффекта — «**снижает** риск single-index omission», НЕ «закрывает
  умалчивание» (см. §1, граница защиты).
- **D3. Strict verification (data_root/Wayfinder) — «защита в глубину»**, не в
  этом плане. Основание — инварианты §1.
- **D4. Доступ к метрикам: `/admin/metrics` (bearer) + прямой
  Cloudflare Analytics Engine SQL API.** Поправка v16 (решение владельца по
  M ревью 18, least privilege): авторизация — **ОТДЕЛЬНЫМ секретом
  `METRICS_ADMIN_SECRET`**, не общим `ADMIN_SECRET`: читатель метрик не
  получает автоматом права seed-invite/revoke — §4.PR-2 «Реализация».
- **D5. Телеметрия — только серверная.**
- **D6. `restore.html` хранится на Arweave и распространяется как СКАЧИВАЕМЫЙ
  ФАЙЛ** с проверкой SHA-256 (реш. 1 §7) — не как исполняемая gateway-страница.
  txId в README и приложении. Это снимает critical-1.
- **D7. Конкретные адреса шлюзов утверждаются ДО production-wiring** (не «в PR
  по ходу»): реальные URL, операторы, CORS, GraphQL schema/pagination, limits,
  status-поведение, sandbox-redirect для HTML. **УТВЕРЖДЕНО владельцем
  2026-08-19 по результатам живых проб — состав в §2.1.** Открытым остаётся
  только `UPLOAD_GATEWAYS` (утверждается по staging-испытаниям в PR-3b).
- **D8. Типы endpoint РАЗЛИЧАЮТСЯ + дедуп по РАЗНЫМ ключам (ревью 2, H5).**
  Индексы — ПОЛНЫЕ публичные HTTPS URL, сгруппированные в ЛОГИЧЕСКИЕ
  источники `INDEX_SOURCES` (поправка v6, ревью 4 M1: primary +
  transport-fallbacks в группе; completeness — по группам, §2.1 и §4.PR-4),
  т.к. индексный endpoint в общем случае несёт путь. Поправка v5:
  утверждённый публичный Goldsky-поиск —
  `https://arweave-search.goldsky.com/graphql` (проверен вживую 2026-08-19,
  форма `origin+/graphql`); Goldsky-subgraph-endpoints вида
  `/api/public/.../gn` существуют как отдельный продукт — обоснование v4
  опиралось на них. Решение «полный URL» остаётся верным для обоих случаев:
  два разных endpoint на одном хосте имеют ОДИН origin, поэтому URL индексов
  канонизируются и дедуплицируются по **полному URL**.
  `STATUS/PAYLOAD/UPLOAD_GATEWAYS` — bare origins, дедуп по **origin**. CSP
  `connect-src` строится ТОЛЬКО по извлечённым origin. Клиентский bearer для
  приватных индексов НЕ используется. (Ошибка v2 «дедуп всех списков по
  origin» отбросила бы один из двух Goldsky-endpoint — исправлено.)

**Решения из security-ревизии (v13, `arweave-gateway-security.md`).** Тот
документ задаёт вопрос «а если шлюз ЛЖЁТ?» там, где этот план спрашивает «а
если шлюз ЛЕЖИТ?». Три его вывода меняют условия выпуска PR-ов этого плана и
поэтому включены сюда как нормативные решения; остальное сведено в §2.2.

- **D9. Верификация `txId ↔ bytes` — ОБЯЗАТЕЛЬНОЕ предусловие
  многогейтвейного payload (F2/R2).** Сегодня клиент проверяет форму JSON,
  `inner-id = Note-Id тег` и AES-GCM, но НЕ проверяет, что байты
  соответствуют запрошенному txId. С одним шлюзом это почти безразлично; с
  пулом `PAYLOAD_GATEWAYS` и правилом «первый валидный ответ побеждает»
  ЛЮБОЙ из N шлюзов может отдать **подлинную, но ДРУГУЮ версию той же
  заметки** (тот же Note-Id → inner-id совпадает → AES-GCM проходит) вместо
  байтов запрошенного txId — тихий downgrade, причём множество тех, кто на
  это способен, растёт с 1 до N. То есть без D9 пул делает integrity-позицию
  СТРОГО ХУЖЕ, а не лучше. Нормативно: **пул payload-шлюзов не выпускается
  без проверки соответствия байтов txId**; не прошедший проверку ответ —
  это отказ ШЛЮЗА (метрика + следующий шлюз), а не «кандидат не
  расшифровался». Практическая цена невелика именно у нас: `MAX_BODY_BYTES =
  51200` (`wrangler.toml:34`) ⇒ данные всегда меньше чанка Arweave (256 KiB)
  ⇒ `data_root` считается несколькими вызовами WebCrypto SHA-256 без
  arweave-js в бандле. **Полный протокол (источник tx-header, схема, лимиты,
  порядок шагов) — §4.PR-3a «Верификация D9»; там же нормативный шаг сверки
  АУТЕНТИФИЦИРОВАННОГО владельца с `TRUSTED_OWNERS`.** Поправка v14 (ревью
  11): формулировка v13 «владелец становится аутентифицированным, что
  закрывает F1» была неполной — подпись доказывает, что владелец ИМЕННО ТОТ,
  чей ключ указан, но НЕ доказывает, что он ДОВЕРЕННЫЙ. Без явной сверки
  адреса корректно подписанная транзакция кошелька атакующего прошла бы всю
  цепочку `id/signature/data_root`; F1 закрывает только шаг 5 протокола.
  **Бюджет бандла — часть гейта:** запас 2.7 KB gz (192.3/195). Если
  верификация в него не влезает — не выпускается ПУЛ, а не проверка;
  одиночный `arweave.net` остаётся до расширения бюджета.
- **D10. Глобальный предохранитель горячего кошелька — предусловие
  writer-релиза PR-3b (F5/R5).** Все существующие ограничители —
  **поштучные**: `RATE_LIMITER` берётся как
  `idFromName(publicKeyB64)` (`index.ts:670`), 20 публикаций/час на ключ
  (`wrangler.toml:35`), `IpRateLimiter` — на IP. Ни счётчика суммарных
  трат, ни порога баланса, ни аларма на скорость сжигания в `worker/src`
  нет (проверено). `MAX_RECOVERY_INFLIGHT` из §4.PR-3b живёт в том же
  per-key DO и агрегат тоже НЕ ограничивает. **Почему это становится
  блокером именно сейчас:** PR-3b впервые делает трату АВТОНОМНОЙ — фаза 2
  `redrop_pending` подписывает и публикует платную транзакцию по DO-alarm,
  без клиента и без человека. Наложите на это оговорку §2.1 о программной
  монокультуре (все пять status-шлюзов — один ar.io-стек, общий баг
  затрагивает всех одновременно) — и строгий N-of-N кворум, чья сила
  опирается на независимость источников, перестаёт защищать ровно в том
  сценарии, который порождает массовый ложный `dead`. Итог:
  коррелированный автономный слив. Нормативно: до writer-релиза PR-3b в
  Worker появляются (а) **порог баланса**, (б) **общий скользящий бюджет
  трат** отдельным глобальным DO (не per-key), (в) **потолок стоимости одной
  транзакции**, (г) **метрика/аларм скорости сжигания**. Координация
  глобального `SpendGuard` с per-key DO — идемпотентная сага (v16, HIGH
  ревью 13: одна транзакция два DO не охватывает). **Денежный и
  атомарный протокол — §4.PR-3b «Предохранитель D10»** (v14, ревью 11 H2:
  версия v13 не задавала единиц, атомарности и идемпотентности, а лимит
  «по числу публикаций» не останавливает одну транзакцию с завышенной ценой).
  Осознанная цена: сработавший предохранитель деградирует ВСЕХ до `503` —
  это верный fail-closed выбор для bearer-актива.
- **D11. Расхождение индексов о НАЛИЧИИ txId: метрика всегда,
  `incomplete` — только по порогу давности (F3/R3, скорректировано).**
  Security-документ предлагает «индексы разошлись о наличии txId →
  `incomplete=true`». В таком виде правило вредно: отставание индекса —
  штатное явление, свежая транзакция закономерно есть в одном источнике и
  ещё нет в другом, поэтому флаг поднимался бы почти всегда, обесценивая
  сигнал и навсегда блокируя продвижение `sweep-full-at` (§1). Нормативно:
  union берётся ВСЕГДА (потеря кандидата недопустима); расхождение ВСЕГДА
  даёт метрику; `incomplete=true` ставится, только если отсутствующий у
  части источников txId **старше порога давности**, при котором отставание
  его уже не объясняет (порог — вместе с `MIN_COMMITTED_AGE_MS`
  калибруется в PR-4 по данным метрик PR-2). Так тихое умалчивание
  становится наблюдаемым, а честный лаг не ломает семантику полноты.

### 2.2 Что из security-ревизии НЕ меняет план

- **R4 («одиночный 404 никогда не `dead`») уже перевыполнен** формулой §4.PR-3a:
  security-документ просит «≥2 согласных источника», план требует строгий
  N-of-N по ВСЕМ настроенным origin, `400` не `dead` никогда, age guard
  сохранён. Отдельной работы нет — это ОДНО изменение, а не два. Важная
  оговорка: независимость источников в кворуме ограничена монокультурой
  (§2.1), поэтому backstop-ом служит D10, а не сам кворум.
- **Раздел «уже безопасно, не трогать»** security-документа не конфликтует с
  планом ни в одном пункте (AES-GCM как аутентификатор, claim-after-decrypt,
  build-time пин owners, отсутствие height-курсора, fail-closed
  `unavailable → 503`, age guard, server-authoritative txId + HMAC-recovery,
  per-key/per-IP лимитеры). Один пункт оттуда поднят в явный инвариант PR-4:
  **restore остаётся АДДИТИВНЫМ upsert-repair** (`storage.ts:295–312`,
  `376–395`) — именно он спасает устройство, у которого новая ревизия уже
  есть локально, от подсунутой старой; переработка union/сортировки в PR-4
  не имеет права это ослабить.

### 2.1 Утверждённый состав D7 и живые пробы (2026-08-19)

Пробы выполнены из среды с сетевым доступом (в отличие от среды, где писался
v4): 13 кандидатов, реальная 10-дневная транзакция проекта (EternalNotes) для
`/raw`, несуществующий валидный txId для status-семантики.

**Утверждённые списки:**

- `STATUS_GATEWAYS` (5 origin): `arweave.net`, `ar-io.dev`, `vilenarios.com`,
  `frostor.xyz`, `permagate.io` — у всех пяти согласованная семантика
  (несуществующий txId → `404`, существующий → `200`), требование ≥2
  status-origin (H4) выполнено с запасом.
- Индексы — **ЛОГИЧЕСКИЕ источники** (`INDEX_SOURCES`, поправка v6 по ревью 4
  M1): completeness считается по логическим источникам, transport-fallback —
  ВНУТРИ группы. Два логических источника:
  - `arweave-index` = [`https://arweave.net/graphql`,
    `https://vilenarios.com/graphql` (transport-fallback того же индекса)];
  - `goldsky-index` = [`https://arweave-search.goldsky.com/graphql`].
  Ошибка v5: vilenarios был записан третьим РАВНОПРАВНЫМ endpoint — при
  строгой семантике `incomplete=false ⟺ все дочитаны` его отказ помечал бы
  sweep неполным при живых обоих независимых индексах, т.е. fallback СНИЖАЛ бы
  доступность. Исправлено: см. §4.PR-4.
- `PAYLOAD_GATEWAYS`: `arweave.net` (первый), `vilenarios.com`, `ar-io.dev`,
  `frostor.xyz`. **`permagate.io` в PAYLOAD НЕ входит** — его холодный `/raw`
  в пробах 19.08 стабильно таймаутил (25s+); 20.08 отдал 200 из кэша —
  нестабильность холодного пути дисквалифицирует из PAYLOAD, в STATUS остаётся.
- `UPLOAD_GATEWAYS`: НЕ утверждён — определяется staging-испытаниями в PR-3b;
  владелец разрешил платные транзакции для этих испытаний (staging JWK).

**Факты проб (19–20.08.2026; колонка `/raw` дополнена CORS-замерами по
ревью 4 M3 — заголовок снят с фактических 200-ответов при `Origin: null`):**

| Кандидат | status | graphql (CORS для `Origin: null`) | `/raw` при `Origin: null` |
|---|---|---|---|
| arweave.net | ok | ok, `ACAO: *` | 200 + `ACAO: *`, ~0.5s |
| vilenarios.com | ok | ok, `ACAO: *` | 200 + `ACAO: *`; холодный ~13s → из кэша ~0.4s |
| ar-io.dev | ok | ok, `ACAO: *` | 200 + `ACAO: *` после прогрева; холодный может 504 (504 — БЕЗ ACAO: для file:// это network error, перебор продолжается) |
| frostor.xyz | ok | ok, `ACAO: *` | 200 + `ACAO: *`; холодный ~12–16s |
| permagate.io | ok | ok, отражает `null` | 19.08 холодный таймаутил (25s+); 20.08 из кэша 200 + `ACAO: *` — нестабилен, вне PAYLOAD |
| arweave-search.goldsky.com | — | ok, отражает `null` | — |
| ar-io.net | DNS не резолвится — исключён | | |
| love4src.com, g8way.io, adaconna.top | не отвечают — исключены | | |
| ariospeedwagon.com, arweave.fllstck.dev, aoweave.tech | нестабильны (5xx/частичные отказы) — исключены | | |

Acceptance-правило (нормативно): каждый шлюз в `PAYLOAD_GATEWAYS` обязан
отдавать `Access-Control-Allow-Origin: *` (или допускать `null`) на
**200-ответах** и graphql, и `/raw`, проверено из реального `file:///`
контекста; не проходит — исключается из PAYLOAD. К STATUS-шлюзам
file://-требование не применяется (restore не делает status-проверок);
им достаточно CORS с origin приложения — все пять отдают `*`.

- Холодный `/raw` вторичных шлюзов (10–16s) укладывается в текущий
  `PAYLOAD_TIMEOUT_MS = 20s` (`arweave.ts:590`); после первого запроса ответ
  кэшируется. Порядок перебора в `PAYLOAD_GATEWAYS` нормативен.
- **Sandbox-redirect (реш. 6 §7) ПОДТВЕРЖДЁН вживую:** `permagate.io/{txId}` и
  сам `arweave.net/{txId}` отвечают `302 → https://<base32(txId)>.<шлюз>/…`;
  `/raw/{txId}` и `/graphql` редиректу НЕ подвержены — текущий CSP не ломается.
- Analytics Engine доступен на Workers **Free**: 100k записей/день, 10k
  read-запросов/день — для PR-2 достаточно с большим запасом.
- Семантика auth для `/admin/metrics` (нет конфига → 503, нет/неверный bearer
  → 401) УЖЕ реализована для существующих `/admin/*`
  (`verifyAdminSecret`, `worker/src/index.ts:186`) — PR-2 переиспользует её,
  обобщив параметром «какой секрет»: bearer `/admin/metrics` сверяется с
  ОТДЕЛЬНЫМ `METRICS_ADMIN_SECRET` (D4, v16), криптографика не дублируется.

**Оговорка независимости / failure domains (в ADR, уточнено ревью 4):**
GraphQL ar.io-шлюзов — прокси к индексу, который проиндексировал сам gateway
(ar.io-node отдаёт только свои данные), для наших целей это ОДИН логический
индекс arweave-семейства; vilenarios в `INDEX_SOURCES` — transport-fallback,
НЕ независимое мнение. Независимых индексных РЕАЛИЗАЦИЙ две — arweave.net и
Goldsky. По status-шлюзам: ОПЕРАТОРЫ различны (ar.io core team — ar-io.dev;
сообщество — vilenarios, frostor, permagate; arweave.net — своя команда), но
ПРОГРАММНАЯ реализация у всех пяти — ar.io-стек, т.е. один software failure
domain: общий баг (например, ложный `404`) затронет всех одновременно.
Именно поэтому dead-формула v6 — строгий N-of-N по ВСЕМ настроенным (§4.PR-3a)
плюс age guard, а не K-of-N по «числу голосов». Byzantine-граница §1 остаётся
в силе.

**Прочие решения владельца (2026-08-19, вторая серия):**

- платные транзакции для staging-испытаний upload-failover — РАЗРЕШЕНЫ;
- verifier restore.html — только ПК (§4.PR-5), мобильный cold-restore —
  отложенная задача в бэклог;
- release-кошелёк создаётся на этапе PR-5; хранение: генерация оффлайн,
  основная копия — менеджер паролей, запасная — бумажная, НИКОГДА в
  repo/CI/облаке в открытом виде и НИКОГДА в самих Eternal Notes
  (циклическая зависимость: этот ключ нужен, когда приложение умерло);
- Cloudflare API token для Analytics Engine SQL API создаётся на этапе PR-2
  (через Claude Chrome), хранится вне репозитория;
- порядок треков — §6 (автотриггеры вставлены после read-path целиком).

## 3. Объём и границы

Уже реализовано (main, PR #53): инкрементальный sweep, sentinel-модель,
суточная страховочная сверка, прицельный ремонт, решение «нет cursor». Этот
план строится ПОВЕРХ и не меняет его семантику.

Сознательно ВНЕ объёма (отдельные ADR):

- bundles/ANS-104 — закрыто до изменения масштаба на порядки;
- AO/HyperBEAM/Solana/ArNS — не в цепочке восстановления;
- Wayfinder SDK — не в PWA-бандл;
- клиентская телеметрия (D5);
- автотриггеры sweep + `/feed` — разблокированы; место в очереди утверждено
  владельцем 2026-08-19: после read-path целиком (PR-3a + PR-4), ДО
  write-path — см. §6;
- **подписанный high-water mark (v13, R3-слой 2 security-ревизии) — отдельный
  ADR, НЕ в этом плане.** Worker подписывает клиенту минимум («у owner-hash H
  не меньше N закоммиченных txId; у корня R ревизия не ниже V»), клиент
  fail-closed уходит в `incomplete`, если индекс вернул меньше. Это
  ЕДИНСТВЕННЫЙ механизм, ловящий downgrade на ЧИСТОМ устройстве и полное
  умалчивание даже при сговоре всех индексов — D11 такого не даёт (там
  расхождение обнаруживается только если источники не согласованы). Условия
  пересмотра: (а) метрика расхождений индексов (D11) показывает
  систематическое умалчивание, ЛИБО (б) появляется требование гарантии
  свежести на чистом устройстве. Жёсткие границы будущего ADR: аттестация
  живёт ровно столько, сколько живёт Worker, поэтому в **автономный
  `restore.html` она НЕ входит** (там опора только на union индексов), и она
  НЕ имеет права вернуть block-height cursor — счётчик/ревизия совместимы с
  бескурсорным полным sweep.

**Возвращено В объём по ревью (было «отдельный ADR»):** сохранение подписанной
транзакции/txId ДО первого POST. Причина — без него безопасный upload failover
невозможен (critical-2). Это ПРЕДУСЛОВИЕ write-path (§4.PR-3b), не опциональное
улучшение.

## 4. Разбиение на PR (порядок = порядок мержа)

### PR-1 «ADR + план»

- `docs/ARWEAVE-RESILIENCE-PLAN.md` (этот файл).
- `docs/adr/0001-...md`: «Arweave — permanent layer, gateway — заменяемый
  transport», решения **D1–D11** (v14: D9–D11 из security-ревизии — trust-,
  spend- и incompleteness-инварианты), privacy trade-off D2 (абзац), **остаточная
  Byzantine-модель полноты (§1)**, список «не делаем» с условиями пересмотра.
- В ADR также: **инвариант same-signed-tx** (уточнено v12, ревью 10:
  write-path не создаёт второй DURABLE/POSTABLE txId без доказанного
  `dead`; эфемерные вызовы signer инвариантом не считаются — §4.PR-3b) и
  **trust-инвариант restore** (байты HTML проверяются в runtime до
  приёма seed).

### PR-2 «Метрики»

**Предусловие (ревью 1+2, M1):** `Arweave.createTransaction()` сам делает
anchor+price запросы внутри SDK — из вызовов только `index.ts` нельзя
раздельно измерить anchor|price|post. Поэтому СНАЧАЛА выделяется явный
**transport adapter** (`worker/src/arweave-transport.ts`): `getAnchor` и
`getPrice` вызываются ЯВНО, их результат **передаётся в `createTransaction`
уже загруженным** (`{last_tx, reward}`), так что SDK больше НЕ делает скрытых
сетевых запросов; `postTx` — отдельная инструментируемая функция. Без этого
метрики транспорта недостоверны.

- `wrangler.toml` (+ **staging vars/bindings**, не только production —
  named staging НЕ наследует production bindings):
  `[[analytics_engine_datasets]] binding = "METRICS"`.
- Схема событий фиксируется ТАБЛИЦЕЙ в `docs/METRICS.md` со стабильным
  порядком blob/double-полей (Cloudflare требует стабильного порядка):
  `gateway_call`(endpoint-тип, хост, класс статуса, latency), `upload_outcome`,
  `status_verdict`.
- **Security-события (v13):** `payload_hash_mismatch` (шлюз отдал байты, не
  соответствующие txId — D9), `index_presence_disagreement` (txId есть у
  одного источника и отсутствует у другого, с признаком «старше порога» —
  D11), `wallet_burn` / `wallet_balance_low` (скорость сжигания и пробитие
  floor — D10; аларм оператору). Все — без содержимого, по общему запрету
  ниже. **Уточнение v16 (решение владельца 2026-08-20 + L ревью 15):** эти
  события ПОЯВЛЯЮТСЯ вместе с кодом соответствующих PR (D9 — PR-3a, D11 —
  PR-4, D10 — PR-3b); `docs/METRICS.md` описывает СТРОГО те события,
  которые реально пишутся после PR-2 (см. «Реализация» ниже), без
  «зарезервированных» строк — имена будущих событий живут здесь, в
  roadmap-разделах.
- **Разделение repost-метрик (ревью 2, M1):** «repost → к нулю» из v2
  двусмысленно — безопасный resend ТОГО ЖЕ txId (PR-3b) и настоящий платный
  redrop (новый txId после dead) — разные события. Метрики:
  `post_attempt_same_tx` (норм. failover, НЕ регрессия), `post_accepted`,
  `redrop_new_tx` (новый txId после подтверждённого dead), `resign_violation`
  (аларм). **Определение `resign_violation` (уточнено v11, ревью 9): второй
  DURABLE/POSTABLE txId у одной версии без НОВОГО доказанного dead** — т.е.
  второй CAS-закоммиченный `signed` в одной generation. Повторный ВЫЗОВ
  signer-а (crash после подписи до CAS, проигравший конкурент фазы 2) —
  НЕ событие: эфемерные подписи отбрасываются до durable-коммита и
  недоступны для POST по построению. **Главный security-KPI = число НОВЫХ
  durable/postable txId на одну версию БЕЗ подтверждённого dead → должно
  быть 0.** **Уточнение v16:** в PR-2 реально пишутся `post_accepted` и
  `redrop_new_tx` (redrop-ветка `doRedrop` существует в коде,
  `index.ts:685–691`); `post_attempt_same_tx` и `resign_violation` НЕ
  пишутся — понятий same-tx-resend и durable-txId в коде ещё нет, они
  появляются в PR-3b (в `docs/METRICS.md` до этого не документируются).
- Отчёты учитывают **sampling** (`_sample_interval` в агрегатах) — иначе
  доли и счётчики занижены.
- Запрет содержимого: ни plaintext, ни seed, ни ключей, ни note content, ни
  сырых идентификаторов.
- Инструментация не может уронить путь запроса (try/catch, best-effort).
- `/admin/metrics` (по ревью): раздельные ответы — **отсутствует конфиг
  сервера → 503**, **отсутствует/неверный bearer → 401**; `Cache-Control:
  no-store`; upstream timeout к SQL API; ограниченный размер результата;
  только белый список отчётов (никакого free-form SQL через Worker).

#### Реализация (2026-08-21)

Перенесено в v16 из плана-спутника (консолидация, шапка документа). «Ревью
13–24» ниже — раунды ревью этой реализационной спецификации; она прошла их
ЦЕЛИКОМ и является нормативной для кода PR-2. Гейт старта кода — в шапке:
актуальная ревизия отревьюена → PR #87 смержен → ветка PR-2 от свежего
`main`.

**Сверка с кодом (2026-08-21, перепроверено по `origin/main` `95be581`) —
контракт репозитория, в котором пишется PR-2:**

- **Node ужесточён**: `.nvmrc` = **24.19.0**, `engine-strict=true` в корневом
  И worker `.npmrc`, `engines: ^24.19.0` в обоих package.json. CI ставит
  Node из `.nvmrc` и остаётся авторитетным; локальная машина с Node < 24
  падает по гейту `npm ci` — гейты тогда прогоняет CI.
- **Тулчейн**: worker — vitest 4.1 + `@cloudflare/vitest-pool-workers` 0.22,
  TypeScript 6, wrangler 4.1xx; клиент — vitest 4, jsdom 30, ESLint 10.
  Worker мигрирован на @noble/ed25519 3.1 + parity-тест клиент↔Worker
  (#104); `worker/src/index.ts` этим не затронут.
- **Мастер-выключатель `UPLOADS_ENABLED`** в wrangler.toml (`:46` prod,
  `:120` staging; используется в `index.ts:902`) — сверх прежних V3/V4.
  На PR-2 не влияет: проверка идёт ДО транспортного блока.
- **Якоря `worker/src/index.ts`**: платный блок `:775-794`
  (`Arweave.init` `:779`, `createTransaction` `:781`, `ARWEAVE_POST_FAILED`
  `:792`); `idFromName(publicKeyB64)` `:670`; `verifyAdminSecret` `:186`;
  `getTxStatusWorker` `:981`; `MIN_COMMITTED_AGE_MS` `:67`;
  мемоизированный `getWalletAddress` `:151-174`; Content-Type-гейт роутера
  `:120`; `rate_limited → 429` `:736`; CORS `:845`; `404/400 → dead` `:988`.
- Прочие якоря: `AnalyticsEngineDataset` в workers-types — `index.d.ts:830`;
  скрытые запросы SDK — `arweave/node/common.js:88,101`; staging-precondition
  в `docs/ROLLBACK.md:273` — исторический след («Read them as history»,
  шапка `ROLLBACK.md:6-16`); placeholder KV staging на месте
  (`wrangler.toml:114`); тестовые якоря — в разделе «Тесты» ниже.

**§R1. `worker/src/arweave-transport.ts` (новый)**

- `assertStructurallyCompleteJwk(raw)` — переименовано по M ревью 15: имя
  честно отражает границу проверки. Полный приватный RSA-набор:
  `kty === 'RSA'` и непустые строки `n, e, d, p, q, dp, dq, qi`; `'{}'` и
  усечённый ключ падают ДО любой сети. **Принятое ограничение (явно):**
  структурно полный, но криптографически невалидный JWK (битый base64url,
  несогласованные параметры) пройдёт проверку, потратит anchor+price запросы
  и упадёт на подписи — это осознанная цена отказа от импорта ключа до сети
  (импорт через WebCrypto не совпадает с RSA-реализацией arweave-js и дал бы
  ложную уверенность). Закрепляется тестом.
- `getAnchor(deps)` / `getPrice(bytes, deps)` — явные `fetch` с
  **`AbortSignal.timeout(10_000)`** (безопасно: оба идут ДО подписания,
  деньги не под риском), потолок тела 1 KiB и runtime-схема ответа: anchor —
  `^[A-Za-z0-9_-]{43,64}$`, price — `^\d{1,20}$`; не прошло — отказ шлюза
  (M7 ревью 14). **Схемы сверены с живым шлюзом (2026-08-21):** реальный
  `/tx_anchor` вернул **64-символьный** base64url (indep-hash блока, не
  43-символьный txId!) — диапазон 43–64 обязателен, «ровно 43» уронил бы
  прод; `/price/51200` вернул `3049039377` — чистые цифры. Мок e2e
  (`'A'.repeat(43)`) остаётся в диапазоне.
- `postSignedTx(arweave, tx, deps)` — **исключительно пассивный секундомер**
  (`performance.now()` до/после) вокруг `arweave.transactions.post(tx)`.
  **НИКАКОГО AbortSignal/deadline вокруг POST до PR-3b** (HIGH ревью 14):
  текущий catch освобождает резервацию (`safeRelease`), а потерянный по
  нашему собственному таймауту ответ НЕ доказывает, что шлюз не принял
  транзакцию — активный таймаут расширял бы окно двойной платной публикации.
  Медленный принятый POST обязан доработать до конца; закрепляется тестом.

`deps = { host, emit }`; host — строка `'arweave.net'`. Инстанс `Arweave.init`
мемоизируется на уровне модуля (образец — `getWalletAddress`,
`index.ts:151-174`). Anchor/price на явном fetch (нужен свой замер; URL
совпадают с уже замоканными в тестах), post — через SDK (переписывание
меняло бы семантику загрузчика без выгоды).

**§R2. Правка `worker/src/index.ts:775-794`**

Единственный платный блок; статус-коды, `safeRelease`, тексты ошибок, и —
отдельно — **отсутствие таймаута POST** не меняются. Новый порядок:
`assertStructurallyCompleteJwk` → `getAnchor` → `getPrice` →
`createTransaction({ data, last_tx, reward })` → `addTag`/`sign` →
`postSignedTx`.

Риск под тест: `reward` считается для ровно того числа байт, которое взял бы
SDK (заниженный reward — отвергнутая сетью транзакция).

Второе место — `getTxStatusWorker` (`:981`): существующий fetch оборачивается
замером, эмитит `gateway_call` (kind `status`) и `status_verdict`; сама
логика вердиктов не меняется (это PR-3a). **Пассивность чтения тела (M
ревью 15):** сегодня вердикт определяется ТОЛЬКО HTTP-кодом; ради
`confirmations` появляется чтение JSON-тела — оно обязано остаться
best-effort: (1) вердикт фиксируется по коду ДО разбора тела, как сейчас;
(2) тело читается под уже существующим 10-с `AbortSignal` с потолком 1 KiB;
(3) любой сбой разбора (malformed, oversized, оборванное тело) даёт
`confirmations = −1` и НЕ меняет вердикт. **Точное поле (L ревью 19):
`number_of_confirmations`** (так его называет контракт Arweave, не
`confirmations`); runtime-тип — конечное безопасное целое ≥ 0, всё иное →
−1. Тесты: валидный 200-ответ → число попало в метрику; malformed /
oversized / медленное тело → вердикт как раньше, −1 в метрике.

`Env` += `METRICS?: AnalyticsEngineDataset`, `METRICS_ENABLED?: string`,
`METRICS_DATASET?: string` — **optional** (gap ревью 19): runtime-контракт
намеренно переживает отсутствие binding/vars, тип обязан это отражать.
**Runtime-семантика `METRICS_ENABLED` (M ревью 16):** запись включается
СТРОГО при `env.METRICS_ENABLED === 'true'`; любое другое значение (false,
мусор, отсутствие var, отсутствие binding) — записи выключены, а основной
запрос НИКОГДА не ломается (телеметрия fail-closed, путь запроса fail-open).
Тесты — матрица `true` / `false` / отсутствует / мусор / нет binding.

**§R3. Схема событий и `docs/METRICS.md`**

Первое blob-поле каждого события — его имя; `indexes: [event]` (низкая
кардинальность, независимая выборка на тип).

| Событие | blobs (по порядку) | doubles |
|---|---|---|
| `gateway_call` | event, kind (`anchor`/`price`/`post`/`status`), host, statusClass (`2xx`/`404`/`4xx`/`5xx`/`timeout`/`network`/**`invalid_response`**) | latencyMs; для kind=`price` вторым — quotedWinston |
| `upload_outcome` | event, outcome (`accepted`/`arweave_error`/`arweave_throw`), appVersion | — |
| `status_verdict` | event, verdict (`alive`/`dead`/`unavailable`), host | confirmations (при 200, иначе −1) |
| `post_accepted` | event, host | — |
| `redrop_new_tx` | event, host | — |

Класс `invalid_response` (M ревью 15): HTTP-успех с телом, не прошедшим
runtime-схему (не-base64url anchor, `12abc` вместо цены), — это протокольный
дефект шлюза, и маскировать его под `network` значило бы прятать именно тот
сигнал, ради которого D9 вводит верификацию.

**Точность `quotedWinston` (L ревью 18):** цена допускает до 20 цифр, а
double теряет точность выше `Number.MAX_SAFE_INTEGER`. Нормативно: в SDK
`reward` передаётся ВСЕГДА строкой (как и сейчас); в метрику число пишется
только при `Number.isSafeInteger(...)`, иначе −1 (та же конвенция «не
записано», что у `number_of_confirmations`). Граничный тест (L ревью 19):
цена = `Number.MAX_SAFE_INTEGER` → число в метрике; на единицу больше → −1;
в обоих случаях `tx.reward` остаётся исходной строкой.

**Границы privacy и остаточные риски (решения владельца по ревью 18):**

- privacy-гарантия «ни идентификаторов, ни содержимого» покрывает **только
  Analytics Engine**; существующие `console.error` продолжают писать
  `noteId` (случайный UUID) и публичный `txId` в Worker logs — фиксируется
  в `docs/METRICS.md` как принятый остаточный риск, чистка — отдельным
  малым PR (в PR-2 платный путь меняется минимально);
- **PR-2 НЕ закрывает** два известных риска существующего кода, и его
  release notes обязаны это сказать прямо (P0 ревью 18): (1) неоднозначное
  исключение POST — принятая шлюзом транзакция с потерянным ответом ведёт к
  `safeRelease` и возможному второму платному POST при повторе клиента —
  закрывает только durable same-signed-tx в PR-3b; (2) одиночный `404/400 →
  dead` от единственного arweave.net (`index.ts:988`) — закрывает только
  кворум PR-3a. Темп подтверждён владельцем: после мержа PR-2 и одной-двух
  недель метрик следующим идёт PR-3a.

**Матрица эмиссии (M ревью 15) — где именно стреляет каждое событие
(якоря по `origin/main` `95be581`):**

| Точка исполнения | События |
|---|---|
| `getAnchor` / `getPrice` вернулись или упали (transport adapter) | `gateway_call` kind=`anchor`/`price` с классом; сбой ведёт затем к `upload_outcome=arweave_error`/`arweave_throw` по существующим веткам 502 |
| `postSignedTx` завершился (внутри блока `:775-794`) | `gateway_call` kind=`post`; при 200/202 — плюс `post_accepted` (то есть `post_accepted` = **принятый шлюзом POST**, до `mark-posted`/`commit`) |
| тот же успешный POST в redrop-ветке (`doRedrop` → `repost`) | дополнительно `redrop_new_tx` |
| терминальный `return` из `handleUpload` — **ТОЛЬКО из веток платного пути** (L ревью 19) | `upload_outcome`: `accepted` = итоговый 200 `/upload` ПОСЛЕ реально выполненного POST этого запроса (commit/anchor/recovery); `arweave_error` = не-2xx от шлюза (ветка 502 `:785-788`); `arweave_throw` = catch (`:790-793`). **Ранние возвраты события НЕ эмитят:** validation 4xx, kill switch 503, rate limit 429, идемпотентный hit (`exists`), reconciliation-ветки без нового POST — иначе метрика перестала бы отвечать на вопрос «чем кончаются платные публикации». Семантика: `upload_outcome` — про ответ клиенту по платному пути, `post_accepted` — про шлюз |
| `getTxStatusWorker` вернул вердикт (`:981`) | `gateway_call` kind=`status` + `status_verdict` |

`quotedWinston` и `confirmations` — безопасные числа без идентификаторов;
именно они дают материал для калибровки `MAX_TX_REWARD`/`PRICE_DEVIATION` и
`MIN_PRESENCE_CONFIRMATIONS` (M5 ревью 14) — при честной оговорке в
`METRICS.md`, что распределение confirmations по ОДНОМУ хосту не заменяет
shadow-метрики кворума из PR-3a/PR-4.

Ни `noteId`, ни `txId`, ни ключей, ни IP. `post_attempt_same_tx` и
`resign_violation` не пишутся (понятий ещё нет в коде — PR-3b); по L ревью 15
их имена живут в roadmap §4.PR-3b, а `docs/METRICS.md` описывает строго то,
что пишется, — без «зарезервированных» строк. Отчёты —
`SUM(_sample_interval)`, не `COUNT()`.

Тип `AnalyticsEngineDataset.writeDataPoint` — `workers-types/index.d.ts:830`;
miniflare 4.x биндинг поддерживает.

**§R4. Тесты**

**Сохранение зелёных.** `upload-validation.test.ts` / `v4-validation.test.ts`
не мокают fetch и полагаются на `ARWEAVE_JWK: '{}'` → 502
(upload-validation: `:118-125`, `:292-294`; v4-validation: **`:80-82`** —
поправка L ревью 22, прежний якорь `:292-294` относился только к
upload-validation). `assertStructurallyCompleteJwk` первой сохраняет 502 и
не даёт им пойти в сеть. E2E — **все три набора** (self-review, finding 5):
`e2e-repost.test.ts:196-198`, `v3-gate-e2e.test.ts:178-180`,
`v4-gate-e2e.test.ts:186-188` — мокают три ноги (`/tx_anchor`,
`/price/\d+`, `POST /tx`, с опциональным `:443`) одноразовыми маршрутами и
проверяют `postTx.calls === 1`; отклонение формы URL в adapter сломает три
файла разом и громко.

**Новые** (по умолчанию `worker/vitest.config.mts`; direct-конфиг — где нужен
override env):

- ровно 3 исходящих вызова на успешный upload; битый и НЕПОЛНЫЙ (без `q`/`dp`)
  JWK → 502 и ноль запросов; **структурно полный, но криптографически
  невалидный JWK** → anchor+price ушли, подпись упала, 502 (фиксация
  принятого ограничения);
- `/price/{n}` для длины, совпадающей с расчётом SDK;
- **медленный, но принятый POST не абортируется** — детерминированно (L
  ревью 15): unit-тест `postSignedTx` с ИНЪЕЦИРОВАННЫМ fetch-моком, который
  (а) утверждает, что ему НЕ передан `signal`, и (б) резолвится 200 после
  короткой фиксированной задержки (например 50 мс) — upload завершается 200
  без второй попытки; никакого «дольше любого разумного дедлайна» и
  реального ожидания;
- malformed anchor (не base64url / 3 KiB тела) и malformed price (`12abc`) →
  отказ шлюза, 502, метрика класса **`invalid_response`** (исправлено по M
  ревью 16 — прежний `network` противоречил схеме событий выше и прятал бы
  протокольный дефект шлюза) — НЕ передача мусора в SDK;
- падающий `writeDataPoint` не меняет ответ; эмиттер не содержит
  `noteId`/`txId`/ключей (unit);
- `/admin/metrics`: 503 без `METRICS_ADMIN_SECRET`; 401 при неверном bearer
  (в т.ч. при подаче ОБЫЧНОГО `ADMIN_SECRET` — секреты разделены); 503 без
  конфига AE; 400 на отчёт вне белого списка; **400 на `hours` вне
  1..168 или нецелое** (M7); **`Cache-Control: no-store` на ВСЕХ ответах,
  включая 4xx/5xx**; 503 на таймаут upstream; **malformed JSON при upstream
  2xx → 502**; форма ответа — стабильный `{ rows }`: parser-тест
  `{meta,data,rows} → {rows:data}`, `data` не массив → 502; snapshot-тест
  SQL-шаблонов фиксирует `quantileExactWeighted` и `FORMAT JSON` (ревью 17);
- **матрица `METRICS_ENABLED`**: `true` пишет; `false`/мусор/отсутствие
  var/отсутствие binding — не пишет и не ломает запрос;
- **матрица `upload_outcome`** (L ревью 19, уточнено ревью 20): платный
  успех → `accepted`; идемпотентный hit → события НЕТ; validation 400 →
  события НЕТ; **kill switch → 503, квота → 429** (`rate_limited` отвечает
  429 — `index.ts:736`) — в обоих случаях события НЕТ.

**§R5. `/admin/metrics` и `wrangler.toml`**

`POST /admin/metrics`, тело `{ report, hours? }`; `hours` — целое 1..168
(дефолт 24). **Авторизация — ОТДЕЛЬНЫМ секретом `METRICS_ADMIN_SECRET`
(решение владельца по M ревью 18, least privilege):** читатель метрик не
получает автоматом права seed-invite/revoke, а будущий мониторинг/дашборд не
потребует передела. Порядок: `!METRICS_ADMIN_SECRET → 503` → bearer против
него → 401 → `!CF_ACCOUNT_ID || !CF_ANALYTICS_TOKEN || невалидный
METRICS_DATASET → 503` (конфиг после аутентификации). `Cache-Control:
no-store` ставится на **все** ответы эндпоинта, включая 4xx/5xx — и
**централизованно, а не в обработчике** (M ревью 19): общая проверка
Content-Type в роутере (`index.ts:120`) отвечает 415 ДО диспатча, поэтому
заголовок навешивается в общем месте для любого ответа с
`url.pathname === '/admin/metrics'` (415, неверный метод, 401, 4xx, 5xx).
Тест: запрос без/с неверным Content-Type → 415 И `no-store`.
Эндпоинт объявляется **server-to-server / operator-only**: текущий CORS не
разрешает заголовок `Authorization` (`index.ts:845`) — это правильный
дефолт и он сохраняется; долгоживущий `METRICS_ADMIN_SECRET` не попадает в
браузерный дашборд, будущему UI — свой backend или Cloudflare Access.
Constant-time-сравнение переиспользуется: `verifyAdminSecret` (`index.ts:186`)
обобщается параметром «какой секрет» — сама криптографика не дублируется.
Секрет ставится `wrangler secret put METRICS_ADMIN_SECRET` в оба окружения.
**Оба новых секрета вносятся в `docs/SECRETS.md`** (self-review, finding 16 —
это конвенция репозитория: контракт секретов с потребителями и blast
radius): `METRICS_ADMIN_SECRET` — читатель метрик, blast radius = read-only
отчёты; `CF_ANALYTICS_TOKEN` — blast radius честно шире: scope Account
Analytics Read не сужается до одного dataset, токен читает аналитику ВСЕГО
аккаунта. **Вместе с двумя новыми строками обновляются (L ревью 24):**
(а) существующая запись `CLOUDFLARE_API_TOKEN` (`SECRETS.md:20`) — её
транзитивный blast radius «через деплой читаются все Worker-секреты» после
PR-2 включает и `METRICS_ADMIN_SECRET`, и `CF_ANALYTICS_TOKEN` (то есть
компрометация deploy-токена раскрывает admin-метрики и account-wide
Analytics Read); (б) раздел Rotation — процедура ротации обоих новых
секретов (`wrangler secret put` + пересоздание CF-токена в дашборде).

**Полный контракт (H2 ревью 15) — реализация не изобретает SQL сама:**

- **Upstream:** `POST
  https://api.cloudflare.com/client/v4/accounts/{CF_ACCOUNT_ID}/analytics_engine/sql`,
  заголовок `Authorization: Bearer {CF_ANALYTICS_TOKEN}`, тело — текст SQL.
  `AbortSignal.timeout(10_000)`; ответ читается с потолком **256 KiB** —
  превышение → 502. Маппинг upstream: timeout/сеть → 503; не-2xx от SQL API
  → 502 (тело upstream НЕ проксируется клиенту — только класс и наша
  строка). **Форма ответа — ОДНА, наша (M ревью 16): upstream 2xx
  валидируется и преобразуется в стабильный `{ rows: [...] }`** из колонки
  таблицы ниже; документ Cloudflare наружу не проксируется никогда.
  Malformed JSON при upstream 2xx (не парсится / нет ожидаемых полей) → 502.
  Тело САМОГО запроса к `/admin/metrics` читается существующим
  `readLimitedBody` с малым потолком (1 KiB) — аутентифицированный эндпоинт
  не освобождён от лимита тела (вопрос 3 ревью 16 — да).
- **`METRICS_DATASET` валидируется как SQL-идентификатор**
  (`^[a-z0-9_]{1,64}$`) при старте обработчика; не прошло → 503. Только
  после этого имя подставляется в шаблон — конкатенация без проверки
  запрещена.
- **Белый список из трёх отчётов** (первый состав; параметр ровно один —
  `hours`, подставляется как проверенное целое):

| report | SQL-шаблон (упрощённо) | Форма ответа |
|---|---|---|
| `gateway_health` | `SELECT blob2 AS kind, blob3 AS host, blob4 AS class, SUM(_sample_interval) AS calls, quantileExactWeighted(0.95)(double1, _sample_interval) AS p95_ms FROM {ds} WHERE index1='gateway_call' AND timestamp > NOW() - INTERVAL '{hours}' HOUR GROUP BY kind, host, class LIMIT 200 FORMAT JSON` | `{ rows: [{kind,host,class,calls,p95_ms}] }` |
| `upload_outcomes` | `SELECT blob2 AS outcome, blob3 AS app_version, SUM(_sample_interval) AS n FROM {ds} WHERE index1='upload_outcome' AND timestamp > NOW() - INTERVAL '{hours}' HOUR GROUP BY outcome, app_version LIMIT 50 FORMAT JSON` | `{ rows: [{outcome,app_version,n}] }` |
| `status_verdicts` | `SELECT blob2 AS verdict, blob3 AS host, SUM(_sample_interval) AS n FROM {ds} WHERE index1='status_verdict' AND timestamp > NOW() - INTERVAL '{hours}' HOUR GROUP BY verdict, host LIMIT 100 FORMAT JSON` | `{ rows: [{verdict,host,n}] }` |

  Каждый шаблон завершается `FORMAT JSON`; стандартный upstream-документ в
  этом формате — `{ meta, data, rows }` (L ревью 17). Нормативный парсинг:
  проверить `Array.isArray(upstream.data)` — иначе 502 — и вернуть наружу
  `{ rows: upstream.data }`; ничего другого из upstream-документа наружу не
  утекает. Parser-тест на `{meta,data,rows} → {rows:data}` обязателен.

  Счётчики — всегда `SUM(_sample_interval)` (выборка!), перцентили — только
  взвешенные (`quantileExactWeighted(…, _sample_interval)` — точная форма из
  SQL-reference Analytics Engine; правка M ревью 17: прежняя
  `quantileWeighted` не выполнилась бы). `LIMIT` захардкожен в
  шаблоне, не параметр. Имя вне списка → 400.
- **`Env` +=** `CF_ACCOUNT_ID: string` (var, оба блока),
  `CF_ANALYTICS_TOKEN: string` (secret) и `METRICS_ADMIN_SECRET: string`
  (secret) — в дополнение к `METRICS`, `METRICS_ENABLED`, `METRICS_DATASET`
  из §R2. **Scope токена — минимальный
  и перечислимый (L ревью 17): `Account → Account Analytics → Read` для
  одного нужного account**, ничего сверх; фиксируется в комментарии
  wrangler.toml и в `docs/ROLLBACK.md` рядом с процедурой создания.

Имя таблицы в SQL берётся из `METRICS_DATASET` — никогда не захардкожено.

`wrangler.toml`, в ОБА блока (naming — M6 ревью 14, **явные разные имена**):

```toml
[[analytics_engine_datasets]]
binding = "METRICS"
dataset = "eternal_notes_metrics"
```

и — исполняемым блоком, не комментарием (L ревью 17):

```toml
[[env.staging.analytics_engine_datasets]]
binding = "METRICS"
dataset = "eternal_notes_metrics_staging"
```

плюс `METRICS_ENABLED = "true"` и `METRICS_DATASET = "<то же имя>"` в
`[vars]` (после `:46`, рядом с `UPLOADS_ENABLED`) и `[env.staging.vars]`
(`:118+`). Секреты — комментарием в блоке `:62+`. CI ловит staging-дрейф
dry-run-ом (`deploy:staging:check`).

Пока `CF_ANALYTICS_TOKEN` не создан — эндпоинт 503, запись метрик работает:
PR мержится до токена.

**§R6. Раскатка — release gate через dev-контур (H3 ревью 15 снят)**

Предпосылка ревью «PR-2 меняет платный путь, а прод с пользователями требует
staging» устарела: **2026-08-20 контур переклассифицирован** (шапка
`docs/ROLLBACK.md:6-16`, план `happy-toasting-pnueli.md`) —
`notes.matamata.dev` теперь **рабочий (dev) контур, всё нынешнее — тестовое**;
боевой прод поднимется с нуля на `.app` по команде «v1 готова». Раздел
«STAGING IS A HARD PRECONDITION» (`ROLLBACK.md:273`) — исторический след
(«Read them as history»).

Гейт поэтому такой:

1. код мержится в main после CI-гейтов + `deploy:staging:check` (dry-run —
   защита от дрейфа конфигурации; `[env.staging]` по §1.3 happy-toasting не
   трогаем);
2. деплой — на **dev-worker** (`Deploy Worker (proxy) — dev`), риск ограничен
   тестовыми данными и dev-кошельком;
3. приёмка там же — обязательный чек-лист dev-smoke (P2 ревью 18):
   (а) новый upload проходит (платный, существующими `smoke-v3.mjs` /
   `smoke-v4.mjs` + `smoke-target.mjs`); (б) **повтор того же запроса не
   создаёт второй POST** (идемпотентность `check-and-reserve` пережила
   рефакторинг); (в) **отдельный подписанный запрос с `recheck: true`** —
   существующие smoke-скрипты делают upload + идемпотентный повтор и НЕ
   вызывают `getTxStatusWorker` (L ревью 20), поэтому status-нога
   проверяется этим дополнительным шагом. **Критерий — факт status-вызова,
   а не код ответа** (info ревью 21): немедленный recheck легитимно может
   вернуть 503 «Recheck deferred» — status-метрика при этом уже записана;
   (г) `/admin/metrics` отвечает и показывает **все четыре** ноги
   (anchor/price/post — от smoke, status — от recheck-запроса). SQL API —
   не строгий read-after-write: появление строк проверяется **ограниченным
   polling-ом с backoff и потолком ~2 мин**, по истечении smoke красный, а
   не бесконечный опрос;
   (д) редеплой предыдущего Worker-а — upload снова проходит (rollback
   отрепетирован, не постулирован);
4. **жёсткий launch gate прод-контура (M ревью 19):** настоящий `.app` НЕ
   открывается для пользователей, пока PR-3a (кворум вместо одиночного
   `404/400 → dead`) и writer-часть PR-3b (durable same-signed-tx) не
   закрыли оба признанных риска платного пути. «Обкатанность на dev» —
   необходимое, но НЕ достаточное условие; гейт записывается в
   `docs/ROLLBACK.md` рядом с процедурой провижининга `.app` (часть 2
   happy-toasting).

Откат = редеплой предыдущего Worker-а: метрики перестают писаться;
**внешний request/response-контракт `/upload` и durable-схема не меняются —
предыдущий Worker просто возвращает прежний SDK-transport** (формулировка
уточнена по L ревью 16: сам транспорт платного пути PR-2 меняет, неизменен
контракт). Секция в `docs/ROLLBACK.md` по образцу существующих.

### PR-3a «Read-path multi-gateway» (status + validated payload fallback)

Безопасно выпускается отдельно — только чтение, платных транзакций не создаёт.

**Конфигурация (D8):**

- `src/lib/gateways.ts`: `INDEX_SOURCES` (список ЛОГИЧЕСКИХ источников,
  каждый — упорядоченный массив полных HTTPS URL: primary + transport-fallbacks,
  см. §2.1 и ревью 4 M1), `PAYLOAD_GATEWAYS`, `STATUS_GATEWAYS` (bare
  origins). Валидация как у `PROXY_URL` (`arweave.ts:30–38`).
- **Канонизация и дедуп по РАЗНЫМ ключам (D8, H5):** URL внутри
  `INDEX_SOURCES` — по полному URL глобально (один URL не может входить в
  две группы; два endpoint одного origin остаются оба);
  `STATUS/PAYLOAD` — по origin (дублированный origin в CSV не создаёт ложный
  «кворум»).
- `postbuild.mjs:39`: `connect-src` = union origin(ов) ВСЕХ списков (для
  индексов — origin, извлечённый из полного URL) + proxy.
- **Пустой env НЕ эквивалентен текущему поведению** (исправление ложного
  утверждения v1): при пустых списках payload/status дефолт — `arweave.net`,
  НО семантика статусов ниже применяется всегда (одиночный 404 больше не
  `dropped`). Это осознанное изменение поведения, а не no-op; фиксируется
  тестом и в ROLLBACK.

**Статус — кворум с точной формулой (ревью 4 H1 — нормативно, без
альтернатив):**

Формула v5 «404 от всех ответивших, ответивших ≥2» была двусмысленна и
допускала dead при `2×404 + 3×timeout` — два голоса из одной программной
монокультуры при трёх молчащих. Нормативная формула v6:

- считаются только уникальные canonical origin ПОСЛЕ дедупа
  (`configuredOrigins`); локально валидируется 43-символьный base64url txId
  (иначе `invalid`, без сети);
- **`dead` ⟺ `deadVotes === configuredOrigins.length` И
  `configuredOrigins.length >= 2`** — КАЖДЫЙ настроенный origin ответил `404`.
  ЛЮБОЙ не-404-исход хотя бы одного origin (timeout, `429`, `5xx`, `400`,
  malformed `200`, сетевая ошибка, неответивший) делает dead НЕДОСТИЖИМЫМ в
  этом раунде → verdict `unavailable` (при отсутствии `200`/`202`).
  Константа `MIN_DEAD_WITNESSES` из v4/v5 УПРАЗДНЕНА — порога «ответивших»
  в runtime больше нет; остаётся build-time `MIN_STATUS_ORIGINS = 2`
  (production build fail-closed, PR-6);
- таблица verdict (нормативная, одна для клиента и Worker):

  | Наблюдение | Verdict |
  |---|---|
  | ≥1 источник `200` (валидное тело) | `confirmed` |
  | ≥1 источник `202`, нет `200` | `pending` |
  | `200` с malformed телом | не засчитывается как alive; для dead-формулы = не-404-исход |
  | ВСЕ настроенные origin ответили `404` (настроено ≥2) | кандидат в `dead` (+ 30-мин age guard Worker-а) |
  | `404` у части, у остальных — любой не-404-исход | `unavailable` |
  | `400`, `429`, `5xx`, timeout | `unavailable` (НИКОГДА не `dead`) |
  | Нормативный пример: `2×404 + 3×timeout` при 5 настроенных | **`unavailable`** (тест) |

- Цена решения осознана: один флапающий шлюз из пяти откладывает dead —
  это безопасная сторона (dead лишь открывает платный redrop; застрявший
  `unavailable` = «повторить позже», денег не тратит). Ослабление до K-of-N —
  только отдельным ADR, где голоса группируются по НЕЗАВИСИМЫМ failure
  domains (софт + оператор), а не по числу origin (§2.1: все пять — один
  ar.io-софт-домен, поэтому счёт origin-голосов не даёт независимости).
- Worker `getTxStatusWorker`: `400` больше НИКОГДА не `dead`; та же формула
  и та же таблица, что у клиента. Бюджет subrequests ≤ (хостов × проверок) в
  лимите Cloudflare.

**Payload fallback (по ревью, high):**

- перебор `PAYLOAD_GATEWAYS` до ПЕРВОГО ответа, прошедшего **верификацию
  `txId ↔ bytes` (D9) + JSON shape + inner-id = tag + AES-GCM** проверку.
  `200` с обрезанным/чужим/повреждённым телом НЕ останавливает перебор —
  успешная валидация второго шлюза нейтрализует ошибку первого. Существующие
  проверки не ослабляются.
**Верификация D9 — полный протокол (v14, ревью 11 H1; нормативно):**

Гейт: пул из >1 payload-шлюза НЕ ВЫПУСКАЕТСЯ без этой проверки. Провал любого
шага — отказ ШЛЮЗА (метрика `payload_hash_mismatch` + следующий шлюз), а НЕ
«кандидат не расшифровался»: иначе подмена неотличима от штатного пропуска
мусора.

- **Источник tx-header:** `GET /tx/<txId>` тех же `PAYLOAD_GATEWAYS`, тот же
  перебор и тот же таймаут (`PAYLOAD_TIMEOUT_MS`), что у `/raw`. Проверено
  вживую 2026-08-20: эндпоинт отдают все четыре payload-кандидата
  (arweave.net, vilenarios.com, ar-io.dev, frostor.xyz) с
  `Access-Control-Allow-Origin: *` в т.ч. при `Origin: null` — значит путь
  работает и из `file://` (PR-5). Header и `/raw` МОГУТ приходить с РАЗНЫХ
  шлюзов: протокол связывает их криптографически, поэтому смешение
  источников безопасно по построению (и даже полезно).
- **Схема и лимиты:** runtime-валидация JSON; жёсткий потолок ответа
  **64 KiB** (реальный header замерен — 2019 байт: `owner` и `signature` по
  683 символа base64url, `data_root` 43, теги base64url); превышение,
  невалидная схема или не-`200` → отказ шлюза, следующий. Все шлюзы
  отказали → кандидат `unavailable` (не «пропущен»), sweep `incomplete=true`
  как сегодня при отказе payload.
- **`format` обязан быть 2** (проверено на реальной транзакции проекта);
  любое другое значение → unsupported → отказ. Писатель проекта никогда не
  создавал format 1, поэтому это fail-closed, а не потеря данных.
- **Порядок шагов (fail-closed на каждом):**
  1. `data_size` из header === фактической длине полученных `/raw` байтов;
  2. `id === base64url(SHA-256(raw-байты подписи))`;
  3. RSA-PSS (SHA-256, salt 32) проверка `signature` над deep-hash полей
     format-2 (`format, owner, target, quantity, reward, last_tx, tags,
     data_size, data_root`); ключ импортируется из `owner` (модуль, `e=AQAB`)
     через WebCrypto;
  4. пересчёт `data_root` по полученным байтам и сверка с header — один
     чанк по построению (`MAX_BODY_BYTES = 51200` ≪ 256 KiB); точная формула
     листа берётся из `merkle.ts` arweave-js и закрепляется тестом против
     реальной транзакции;
  5. **`address = base64url(SHA-256(base64url-decode(owner)))` ∈
     `TRUSTED_OWNERS`** — отдельный обязательный шаг. Шаги 2–4 доказывают
     лишь самосогласованность транзакции и авторство УКАЗАННОГО владельца;
     доверенность владельца доказывает только этот шаг. Сравнение
     каноническое (обе стороны — нормализованный base64url без padding);
  6. и ТОЛЬКО ЗАТЕМ существующее: JSON shape → `inner-id = Note-Id тег` →
     AES-GCM. Ни одна из существующих проверок не ослабляется.
- **Атрибуция после D9 берётся из ВЕРИФИЦИРОВАННЫХ тегов header, а не из
  GraphQL-ребра.** Подпись покрывает теги, поэтому `Note-Id`, `App-Name`,
  `App-Version` принятого кандидата читаются из проверенного header.
  GraphQL низводится до **дискавери** — списка txId, которые стоит
  проверить. Неверифицированные теги индекса разрешено использовать ТОЛЬКО
  для сокращения работы (префильтр чужого `App-Name`, пропуск sentinel-ов) и
  НИКОГДА для атрибуции: ошибочный префильтр может лишь добавить работы или
  пропустить то, что индекс и так волен не показать (это F3/D11, отдельная
  граница), а атрибуция подделке недоступна.
- **Цена и производительность:** один header-запрос на КАЖДОГО незнакомого
  кандидата. Обязательны: общий deadline с payload-фазой, та же ограниченная
  параллельность, кэш уже проверенных txId в пределах прогона; известные
  (sentinel/`known`) кандидаты не качаются вовсе — как сегодня.
- **F1 закрывается шагом 5**, а не «побочно». В этом же PR правится вводящий
  в заблуждение комментарий `arweave.ts:484–486` (§1).
- `isArweaveOnline` → «жив хотя бы один status-шлюз» (`Promise.any`).

**Тесты PR-3a:**

- `404`+`202` → pending; **`2×404 + 3×timeout` (5 настроенных) → unavailable,
  НЕ dead** (нормативный пример формулы); `404` от всех настроенных → dead
  только после age guard; `400` → unavailable, redrop не запускается;
- два `404` от шлюзов одной failure-domain группы + один здоровый origin
  без ответа → unavailable;
- `/raw` `200` с повреждённым телом на первом, валидный на втором → нота
  восстановлена, ошибка первого нейтрализована;
- **D9 (security):** шлюз отдаёт для txId `X` байты, не соответствующие `X`
  (случайные И подлинные байты ДРУГОЙ версии того же Note-Id) → оба отвергнуты
  ДО расшифровки как отказ шлюза, счётчик `payload_hash_mismatch`, перебор
  продолжен; подмена подлинной старой версией НЕ побеждает в цепочке;
- **D9 шаг 5 (ключевой тест ревью 11):** транзакция, **корректно подписанная
  кошельком атакующего** — `id`, `signature`, `data_root`, `data_size` сходятся
  идеально — отвергается на сверке адреса с `TRUSTED_OWNERS`; ни расшифровки,
  ни занятия Note-Id не происходит;
- **D9 транспорт:** header и `/raw` получены с РАЗНЫХ шлюзов → принимается
  (связь криптографическая); malformed header, header >64 KiB,
  `format ≠ 2`, `data_size` ≠ длине байтов → отказ шлюза + следующий; все
  шлюзы отказали → кандидат `unavailable`, sweep `incomplete=true`;
- **D9 атрибуция:** GraphQL-ребро объявляет `Note-Id` A, верифицированные
  теги header — `Note-Id` B → используется B (ребро — только дискавери);
- **F1-регрессия:** ребро с чужим владельцем (шлюз проигнорировал
  `owners:`-фильтр) не приводит к показу заметки и не занимает Note-Id ни при
  каких обстоятельствах;
- дублированный origin в CSV не даёт кворум;
- невалидный txId → invalid без сети;
- пустой env → снапшот CSP + явный тест на ИЗМЕНЁННУЮ семантику 404;
- lock/reset отменяет запросы ко всем шлюзам.

### PR-3b «Write-path upload failover» (отдельно, строгий протокол)

**Critical-2 (принято): текущий план v1 создавал вторую платную транзакцию.**
`create+sign+post` на втором хосте = новая подпись = новый txId; `5xx`/timeout
не доказывает, что первый шлюз не принял транзакцию → две платные публикации
одной версии. Прямо нарушает P0-инвариант исходного отчёта.

**Протокол (нормативный):**

1. anchor и price можно переключать между шлюзами ДО подписания.
2. **Инвариант generation (переформулирован v11, ревью 9): ровно один
   DURABLE/POSTABLE txId на generation.** Денежный инвариант защищает CAS,
   а не счётчик вызовов signer-а: exactly-once ВЫЗОВА подписи недоказуем
   (конкурентная фаза 2 или crash после подписи до CAS законно вызывают
   signer повторно) и НЕ требуется. Нормативно: durable-коммит `signed` с
   новым txId возможен только у CAS-победителя; **POST разрешён
   исключительно для durable-закоммиченного txId**; эфемерные подписи
   проигравших/оборванных попыток ОТБРАСЫВАЮТСЯ и не POST-ятся никогда.
   Новая generation открывается ЕДИНСТВЕННЫМ путём — переходом
   `signed → redrop_pending` (фаза 1, dead + age guard доказаны).
3. **Предусловие:** подписанная транзакция и её txId сохраняются в DO ДО
   первого POST. Потерянный ответ реконсилируем, а не повод для второй подписи.
4. Failover POST = повторная отправка ТОГО ЖЕ сериализованного transaction с
   ТЕМ ЖЕ txId на другой шлюз. `208 Already Reported` — допустимый ответ POST
   /tx, трактуется как принято.

**Нормативная state machine DO (ревью 2 H2, ревью 3):**

Текущий lifecycle `reserved → posted → committed` (reserved TTL 10 мин)
расширяется до `reserved → signed → posted → committed`, плюс durable
side-состояние **`redrop_pending`** (v9): петля `signed → redrop_pending →
signed(новый txId)` — единственный путь второй подписи (детали ниже).

**Storage-контракт `signed` (ревью 4 H2 — нормативно, без альтернатив):**

- Сериализованная подписанная транзакция хранится **INLINE в DO storage** —
  вариант `|ref` из v5 УПРАЗДНЁН (внешняя ссылка = атомарность и потеря
  reference, которые пришлось бы доказывать). Запись
  `note:{noteId} = {status:'signed', txId, signedTx, anchorHeight, token,
  gen, signedAt, dueAt, attempts, deadTxId?, spendKey, reward,
  spendRevision}` — ОДИН атомарный `storage.put` (последние три поля — v17:
  durable-привязка саги D10, аргументы идемпотентного
  `activate(spendKey, reward, revision)`; см. «Предохранитель D10»)
  (`deadTxId?` — опциональный lineage, v11 ревью 9: у свежего upload-а
  отсутствует, после redrop сохраняется в новом `signed` для аудита цепочки
  generations). Влезает по
  построению (поправка v7, ревью 5 low): `RateLimiter` — **SQLite-backed DO**
  (`worker/wrangler.toml:14`, `new_sqlite_classes`), лимит key+value —
  **2 MB** (128 KiB из v6 — лимит legacy KV-backed DO, к нам не относится);
  наш собственный worst case при `MAX_BODY_BYTES = 51200` — сериализованный
  tx-JSON (data + owner ~0.7 KB + signature ~0.7 KB + tags) ≤ ~75 KB — запас
  более чем 25×. Тест фиксирует худший случай против ВНУТРЕННЕГО предела
  ~75 KB, не против платформенного.
- **Порча/потеря `signedTx` — fail closed:** запись `signed` есть, а байты
  не парсятся/не сходятся с txId → resend невозможен, слот НЕ освобождается
  автоматически; допустима только reconciliation по кворуму статуса (alive →
  `posted`; кворум dead + age guard → redrop-ветка, где новая подпись
  легальна). noteId разблокируется ТОЛЬКО этими двумя путями.

**Проблема ревью 3: одно `signed` покрывало ДВА случая (до и после диспатча
POST), и после crash нельзя доказать, какой именно → нельзя доказать,
допустим ли release. Нормативное решение (без альтернатив): `signed`
НИКОГДА не подлежит release и НИКОГДА не TTL-expire.** Как только транзакция
подписана, единственные допустимые действия — resend ТОГО ЖЕ txId и
reconciliation по кворуму статуса. Это снимает необходимость различать
«до/после POST»: оба случая безопасны под одним правилом, а окно дубля
закрыто по построению (release, стирающий запись о возможно-принятом txId,
недостижим).

| Состояние на момент сбоя | Что сделать при recheck |
|---|---|
| `reserved`, подписи ещё нет | обычный TTL-expire → свободный слот, дубля нет |
| `signed` (любой под-случай) | **release/TTL ЗАПРЕЩЕНЫ**; только resend того же txId + reconciliation по кворуму → при alive записать `posted` |
| `redrop_pending` (v9: фаза 2 не завершена — crash/ошибка между фазами) | **release/TTL ЗАПРЕЩЕНЫ**; scheduler-visible, backoff + alarm → повторить фазу 2 (новая подпись легальна: dead старого txId уже доказан в фазе 1) |
| `posted` | как сегодня: кворум alive→commit, dead→redrop (новая подпись только тут) |
| `committed` | идемпотентный happy-path |

- **Освобождение слота для `signed`:** не по TTL, а ТОЛЬКО через переход
  вперёд (`posted`/`committed`) либо после доказанного окончательного отказа
  (кворум dead + age guard) — это единственный путь, где создаётся новая
  подпись.
- **Bounded backlog (ревью 4 H2):** «неудаляемый `signed`» без границы —
  storage/cost DoS. Часовое окно для этого НЕ годится: `count/inFlight/attempts`
  обнуляются при смене окна (`rate-limiter.ts:65–71`, проверено) — заброшенные
  `signed` пережили бы сброс неучтёнными. Поэтому отдельный persistent-счётчик
  **`recoveryCount`** в DO (поправка v9, ревью 7: НОРМАТИВНО ОДИН счётчик на
  ОБА recovery-состояния — инвариант: равен числу `note:*` со `status ∈
  {'signed','redrop_pending'}`; меняется в тех же атомарных транзакциях, что
  и записи; `signedCount` из v6–v8 переименован и расширен — иначе
  `signedCount--` в фазе 1 redrop открывал бы новые upload-ы при
  накоплении зависших redrop-записей, возвращая storage/cost DoS). Cap
  нормативно: **`MAX_RECOVERY_INFLIGHT = quotaLimit`** (часовая квота
  пользователя). При достижении cap новый upload отклоняется **ДО
  подписания** (`503`, retryable) — recovery-записей, а значит и
  durable/postable txId, сверх границы не существует по построению
  (уточнено v12: число ЭФЕМЕРНЫХ вызовов signer cap не ограничивает и не
  обязан — они не порождают ни storage, ни POST).
- **Reconciliation — сервер-инициированная, два триггера (ревью 4 H2):**
  (а) как сегодня — любой `/upload`/recheck этого noteId; (б) DO alarm через
  scheduler ниже. Клиент, который никогда не повторит запрос, больше не
  оставляет `signed` навечно — DO добивает сам.

**Single-alarm scheduler (ревью 5 H1 — нормативно, без альтернатив):**

У Durable Object есть РОВНО ОДИН alarm (`setAlarm` перезаписывает предыдущий).
Формулировка v6 «alarm ставится при создании каждой записи и снимается при
переходе вперёд» при нескольких `signed` перезаписывала бы расписание чужой
записи, а завершение одной ноты отменяло бы reconciliation остальных.
Нормативная модель — один persistent scheduler на DO:

- **Scheduler-visible множество (поправка v9, ревью 7) = записи со
  `status ∈ {'signed','redrop_pending'}`** («recovery-записи»). Везде ниже,
  где v7–v8 говорили «signed-записи», нормативно читается это множество —
  промежуточное redrop-состояние НЕ выпадает из расписания.
- **`dueAt` живёт В САМОЙ recovery-записи** (поле, см. storage-контракт)
  вместе с `attempts`; отдельной очереди-структуры нет — источником
  расписания являются сами записи. Backlog ограничен `MAX_RECOVERY_INFLIGHT`,
  поэтому выбор min(dueAt) перебором recovery-записей — O(cap), приемлемо.
- **Глобальный alarm всегда = min(dueAt) по всем recovery-записям.**
  ЛЮБАЯ мутация их множества (создание, переход вперёд, обе фазы redrop,
  изменение dueAt после прогона) завершается пересчётом min(dueAt) и
  `setAlarm(min)` — либо `deleteAlarm()`, когда recovery-записей не осталось.
  Продвижение одной записи по построению НЕ отменяет reconciliation других.
- **Атомарность (ревью 5, уточнено ревью 6 H1 — нормативно, БЕЗ
  альтернатив):** ЕДИНСТВЕННЫЙ механизм crash-атомарности —
  **`storage.transaction()`** SQLite-backed DO. Формулировка v7
  «transaction / blockConcurrencyWhile» была ошибкой: `blockConcurrencyWhile`
  лишь запрещает interleaving других событий, но несколько `await put()`
  внутри него фиксируются ПО ОТДЕЛЬНОСТИ и crash между ними оставит частичное
  состояние (в InviteManager он используется именно для сериализации, не для
  атомарного коммита). Нормативно: КАЖДАЯ scheduler-мутация — создание
  `signed`, переход вперёд, обе фазовые границы redrop — это один
  `storage.transaction`, внутри которого через транзакционный контекст
  выполняются ВСЕ связанные операции: `note:*`, `recoveryCount`, пересчёт
  min(dueAt) и `setAlarm`/`deleteAlarm`. `blockConcurrencyWhile` как средство атомарности
  из плана УДАЛЁН (для сериализации он остаётся допустим, но атомарность
  доказывается только транзакцией). Тест с искусственным crash после каждого
  storage-вызова обязан показать полный rollback («всё или ничего»).
- **Alarm-обработчик:** выбирает записи с `dueAt <= now`, обрабатывает
  ПОСЛЕДОВАТЕЛЬНО (никаких параллельных resend), максимум **`ALARM_BATCH = 5`**
  записей за прогон — бюджет subrequests: ≤5 записей × (resend + ≤5
  status-хостов) в лимите Cloudflare; необработанные due-записи получают
  немедленный re-alarm.
- **Отказоустойчивость обработчика (ревью 6 M1 — нормативно):**
  - каждая due-запись обрабатывается в СОБСТВЕННОМ `try/catch` — исключение
    одного gateway-запроса/записи логируется, запись получает reschedule с
    backoff, обработка остальных записей batch-а ПРОДОЛЖАЕТСЯ;
  - весь прогон обёрнут во внешний **`try/finally`**: `finally` ВСЕГДА
    перечитывает backlog и ставит следующий alarm (`setAlarm(min(dueAt))` /
    `deleteAlarm` при пустом) — даже если прогон упал до этой точки. Cloudflare
    авто-ретраит упавший alarm-хендлер ограниченное число раз (~6 с
    backoff), полагаться на это НЕЛЬЗЯ: перепланирование — обязанность
    самого хендлера ДО возврата;
  - сбой самого финального `setAlarm` не должен осиротить backlog:
    ограниченный retry в `finally` + **self-healing инвариант** — на входе
    КАЖДОГО DO-запроса (`/check-and-reserve`, `/upload`-recheck и т.д.):
    если `recoveryCount > 0`, а alarm не установлен (`getAlarm() === null`) —
    восстановить `setAlarm(min(dueAt))`. Счётчик покрывает и
    `redrop_pending` (v9), поэтому зависшая фаза 2 тоже чинится. Триггер (а)
    reconciliation тем самым восстанавливает расписание даже после
    исчерпания всех platform retry.
- **CAS после КАЖДОГО внешнего await (ревью 5):** перед применением перехода
  запись перечитывается; переход применяется ТОЛЬКО при совпадении
  `{status:'signed', token, txId}` с тем, что было прочитано до сетевого
  вызова. Не совпало (запись продвинул конкурентный recheck или другой
  прогон) — результат ОТБРАСЫВАЕТСЯ без записи.
- **Redrop — ЯВНО двухфазный, через durable `redrop_pending` (ревью 6,
  переписано в v9 по ревью 7 H1):** подпись и сеть НИКОГДА не выполняются
  внутри storage transaction. Ошибка v8: фаза 1 освобождала слот
  (`signedCount--`) и выводила запись из scheduler-видимости — crash или
  падение фазы 2 оставляли «повторный проход» ничем не запланированным
  (последняя запись могла снести alarm), а cap переставал считать зависшие
  redrop-записи (вопрос ревью 7 про `recoveryCount` — да, нормативно один
  счётчик, см. bounded backlog выше). Нормативно v9:
  1. **Фаза 1** — атомарный `storage.transaction`: CAS-проверка
     `{status:'signed', token, txId}` (кворум dead + age guard уже получены
     ВНЕ транзакции) → `signed → redrop_pending` (новый `gen`/token, поле
     `deadTxId := txId` мёртвой транзакции, СВОЙ `dueAt` + backoff-`attempts`).
     **Durable-источник для фазы 2 (ревью 8 M2 — нормативно): поле
     `signedTx` при переходе СОХРАНЯЕТСЯ в записи** — старая подписанная
     транзакция остаётся ЕДИНСТВЕННЫМ durable-источником encrypted payload
     и тегов; ничего внешнего фазе 2 не нужно. **Capacity НЕ
     освобождается** — `recoveryCount` не меняется; запись остаётся
     scheduler-visible и в cap.
  2. **Фаза 2** — ВНЕ транзакции, автономно (клиент не требуется):
     (а) валидация сохранённого `signedTx`: парсится И его txId ===
     `deadTxId` — иначе fail closed (ниже); (б) **data и tags извлекаются из
     сохранённого `signedTx` и переносятся в новую транзакцию БЕЗ
     изменений** (тот же конверт, тот же Note-Id/App-Version — envelope
     не пересобирается); новые только anchor и reward (transport adapter
     PR-2); (в) подпись; (г) атомарный CAS-переход `redrop_pending → signed`
     с НОВЫМ txId и НОВЫМ `signedTx` (транзакция «создание signed» штатного
     цикла; `recoveryCount` не меняется — слот тот же; старый `signedTx`
     замещается, `deadTxId` остаётся для аудита). **POST нового tx разрешён
     ТОЛЬКО ПОСЛЕ durable-коммита нового `signed`** (инвариант «сначала
     запись, потом сеть» сохраняется и здесь).
  3. Ошибка/crash фазы 2 → запись остаётся `redrop_pending` с НЕТРОНУТЫМ
     старым `signedTx`, получает backoff-`dueAt` и новый alarm
     (finally/self-healing её видят через `recoveryCount`); повторный прогон
     повторяет фазу 2 из тех же durable-данных — signer при этом законно
     вызывается ПОВТОРНО (v11): подпись оборванной попытки эфемерна и
     POST-able не была. Конкурентные попытки фазы 2 разрешаются CAS-ом —
     durable-коммитится и отправляется ТОЛЬКО победитель; подписи
     проигравших отбрасываются и не POST-ятся никогда (POST требует
     durable-коммита — недостижим для эфемерной подписи по построению).
  4. **Порча сохранённого `signedTx` в `redrop_pending`** (не парсится /
     txId ≠ `deadTxId`) → автономная фаза 2 НЕВОЗМОЖНА, fail closed: слот
     не освобождается, подпись из мусора не создаётся; лог + метрика.
     Восстановление — только клиентский триггер (а): повторный `/upload`
     этого noteId несёт data в теле запроса и реконсилируется под тем же
     CAS/generation-правилом.
  Вторая подпись легальна ТОЛЬКО в фазе 2, после доказанного dead в фазе 1.
- **Вердикты alarm-прогона (ревью 5 M1) — нормативная таблица («не-alive»
  из v6 упразднено как смешение трёх исходов):**

  | Кворум-статус (формула PR-3a) | Действие |
  |---|---|
  | `confirmed` (alive) | CAS → `posted`, дальше обычный путь |
  | `pending` | reschedule (экспоненциальный backoff с потолком) |
  | `unavailable` | reschedule (backoff) |
  | `dead`, age guard НЕ пройден | reschedule |
  | `dead` + age guard пройден | двухфазный redrop (см. выше): фаза 1 — CAS `signed → redrop_pending` в транзакции (capacity не освобождается); фаза 2 — новая подпись ВНЕ транзакции → CAS `redrop_pending → signed`. ЕДИНСТВЕННОЕ место новой подписи |

- **Reader-релиз (floor) обязан нести ВЕСЬ scheduler-контракт** — dueAt,
  `recoveryCount`, alarm-обработчик, CAS, verdict-таблицу, **включая
  возобновление `redrop_pending` (фаза 2, ревью 7)** — а не только чтение
  `signedTx`; иначе откат writer→reader оставил бы запланированные
  recovery-записи без reconciliation. Reader отличается от writer ровно
  одним: НЕ создаёт НОВЫЙ `signed` для нового upload-а (возобновлять
  существующие recovery-записи, включая фазу 2, он обязан).
- **Истечение anchor:** подписанная tx с протухшим anchor (~50 блоков) на
  chain не попадёт. Пересоздание — только после подтверждения кворумом, что
  старый txId не alive. Это частный случай «доказанного окончательного
  отказа» выше, отдельная ветка с тестом.

5. Если SDK не гарантирует resend той же подписи — **fail closed ДО первого
   POST** (не создавать `signed`), без повторного подписания. После создания
   `signed` release НЕДОСТИЖИМ по определению статуса.

**Предусловие D10 (v13, security-ревизия F5/R5) — глобальный предохранитель
кошелька ДО writer-релиза.** PR-3b — первый механизм, который тратит AR
**автономно** (фаза 2 по DO-alarm, без клиента и человека). Все существующие
лимиты поштучные (`RATE_LIMITER` = `idFromName(publicKeyB64)`, 20/час на ключ;
`MAX_RECOVERY_INFLIGHT` живёт в том же per-key DO), агрегата нет вообще. При
монокультуре status-шлюзов (§2.1) один общий баг ar.io способен дать
одновременный ложный `dead` по всем источникам — строгий N-of-N в этом
сценарии не спасает, потому что его сила в независимости. Поэтому writer-релиз
НЕ выпускается, пока в Worker нет предохранителя по протоколу ниже.
Reader-релиз этим не блокируется: он не создаёт `signed` и не открывает фазу 2.

**Предохранитель D10 — денежный протокол (v14, ревью 11 H2; нормативно):**

- **Единицы — Winston** (1 AR = 10¹² Winston), НЕ «число публикаций»: лимит
  по количеству не останавливает одну транзакцию с завышенной ценой. Три
  независимых предела (значения и порядок их изменения — ниже):
  `WALLET_FLOOR_WINSTON` (неприкосновенный остаток),
  `SPEND_WINDOW_CAP_WINSTON` (скользящее окно трат), `MAX_TX_REWARD_WINSTON`
  (потолок ОДНОЙ транзакции).
- **Новый глобальный DO `SpendGuard`** (`idFromName('global')`) — отдельно от
  per-key `RATE_LIMITER`. Требует своего migration-тега и биндингов в
  production И staging (PR-6), подчиняется тому же reader-before-writer и
  rollback floor.
- **Ключ резервирования — `{publicKeyB64, noteId, gen}` (поправка v15,
  ревью 12 H1: ключ ОБЯЗАН включать tenant), а НЕ txId.** Он известен ДО
  подписания (иначе нельзя отказать до траты) и **не меняется при failover**:
  повторная отправка ТОГО ЖЕ подписанного tx попадает в ту же резервацию и
  списывается ОДИН раз. Redrop открывает новую generation (§4.PR-3b) → новую
  резервацию — то есть денежный учёт совпадает с инвариантом «один
  durable/postable txId на generation» ровно по построению.
  **Почему tenant обязателен.** Сегодня одинаковые `noteId` у разных
  пользователей безопасны ТОЛЬКО потому, что `RATE_LIMITER` расщеплён по
  `idFromName(publicKeyB64)` (`index.ts:670`); глобальная уникальность
  `noteId` НЕ обеспечивается — Worker проверяет лишь формат UUID и класс
  версии (`index.ts:638–645`), так что аллоулистнутый клиент волен прислать
  чужой `noteId`. В ГЛОБАЛЬНОМ DO ключ без tenant означал бы попадание в
  чужую идемпотентную резервацию: повторный `reserve` вернул бы её без
  списания — пересечение границы арендатора, чужой DoS и обход общего
  бюджета. `publicKeyB64` для ключа берётся ТОЛЬКО аутентифицированный
  (Ed25519 над телом уже проверен, `index.ts:486`), никогда «как
  прислали». В метриках сырой ключ не публикуется (общий запрет §4.PR-2) —
  только производный бакет.
- **Повтор `prepare` с тем же ключом (ревью 12, gap):** совпадение tenant +
  `noteId` + `gen` + `reward` + прочих неизменяемых параметров →
  идемпотентный возврат существующей резервации (и её ТЕКУЩЕГО `revision`,
  v17). **Расхождение `reward`
  разрешается по состоянию generation:** пока подписи для этой generation
  НЕТ, допускается атомарная пере-резервация на новую сумму (release + reserve
  в ОДНОЙ транзакции, `revision` инкрементируется — именно этот инкремент
  делает гонку «пере-резервация между `prepare` и `activate`» видимой для
  CAS `activate`, v17; цена могла честно измениться между попытками); как
  только `signed` создан, `reward` вшит в подписанную транзакцию и любое
  расхождение — нарушение контракта → **fail-closed** (`503` + метрика), а НЕ
  «вернуть сохранённое». Расхождение tenant при совпавших `noteId/gen` —
  всегда fail-closed.
- **Сага вместо cross-DO транзакции (v16, HIGH ревью 13) — нормативно.**
  `SpendGuard` — глобальный DO, `RateLimiter` — per-key (`index.ts:670`);
  одна `storage.transaction()` два объекта НЕ охватывает — формулировки v15
  «в той же транзакции, где запись переходит в `redrop_pending`» были
  технически невозможны. Координация — **идемпотентная сага**, ключ
  `spendKey = {publicKeyB64, noteId, gen}` (tenant-правила выше).
  Нормативная таблица состояний (единственная таблица D10):

  | Состояние | Что означает | Истечение |
  |---|---|---|
  | `prepared` | reward зарезервирован, подписи ЕЩЁ НЕТ | ограниченный lease — истёк, резервация освобождается |
  | `active` | per-key DO durable-закоммитил `signed` со `spendKey` и `reward` | TTL ЗАПРЕЩЁН навсегда |
  | `settled` | `spent` (в скользящее окно) либо `released` (доказано, что не опубликовано) | — |

  Lease допустим РОВНО для `prepared`: там доказуемо нет ни подписи, ни
  postable-транзакции — та же логика, что `reserved → TTL` против
  `signed → никогда`.
- **`prepare(spendKey, reward)`** (в v14–v15 называлось `reserve`) — ОДНА
  `storage.transaction` внутри `SpendGuard`: идемпотентно по ключу (повтор
  возвращает существующую резервацию, ничего не списывая); затем три
  проверки, все fail-closed:
  `reward ≤ MAX_TX_REWARD_WINSTON`;
  `rollingSpend + pendingReservations + reward ≤ SPEND_WINDOW_CAP_WINSTON`;
  `balance − (pendingReservations + reward) ≥ WALLET_FLOOR_WINSTON` — **сам
  резервируемый reward входит в вычитаемое**, иначе транзакция, которая
  пробивает floor собственной стоимостью, проходила бы (дефект наивной
  проверки `balance ≥ floor`). Параллельные запросы РАЗНЫХ ключей
  сериализуются этой же транзакцией, поэтому «проверил–и–отправил» гонки нет.
  **Возвращает монотонный `revision` (v17):** счётчик пере-резерваций этого
  ключа — идемпотентный повтор возвращает ТЕКУЩИЙ `revision`, каждая
  пере-резервация (см. «Повтор `prepare`») его инкрементирует; вызывающий
  ОБЯЗАН пронести `revision` до durable `signed`.
- **Порядок саги в Worker:** per-key `check-and-reserve` → котировка цены
  (transport adapter, PR-2 — `getPrice` уже вызывается ЯВНО) →
  `prepare(spendKey, reward) → revision` → подпись → durable `signed` (несёт
  `spendKey`, `reward` И `spendRevision`) →
  `activate(spendKey, reward, revision)` → **только теперь POST** →
  `settle`. Тот же `reward`, что зарезервирован, передаётся в
  `createTransaction` — расхождение котировки и резервации запрещено.
- **Привязка `activate` к резервации (v17, HIGH ревью по v16) —
  нормативно.** `activate(spendKey)` без параметров допускал гонку: резерв
  100 → конкурентный повторный `prepare` на 1 (до подписи это законно) →
  подписана транзакция на 100 → активирован учёт на 1 — занижение
  `pendingReservations` и обход cap/floor. Поэтому `activate(spendKey,
  reward, revision)` — атомарный CAS в ОДНОЙ `storage.transaction`
  `SpendGuard`: `prepared → active` переходит ТОЛЬКО резервация, совпавшая
  и по `reward`, и по `revision` (аргументы — из durable `signed`, не из
  памяти обработчика). Несовпадение (конкурентная пере-резервация между
  `prepare` и `activate`) и отсутствие резервации (lease истёк)
  обрабатываются ОДНОЙ ветвью: атомарная пере-резервация на `reward` ИЗ
  `signed` под всеми тремя проверками `prepare` + метрика (roadmap PR-3b:
  `activate_remap`); бюджета/floor не хватает → `activate` отказывает, POST
  не делается, backoff. Сходимость: конкурент, чья резервация вытесняется,
  свою подпись durable-закоммитить уже не может (durable `signed` — только
  у CAS-победителя per-key DO), поэтому его резервация — сирота, и учёт
  обязан сойтись к `reward` единственного `signed`. Занижение учёта против
  подписанной суммы недостижимо по построению.
- **Crash-границы саги:** после `prepare` до `signed` — lease чинит (терять
  нечего); после `signed` до `activate` — scheduler повторяет идемпотентный
  `activate(spendKey, reward, revision)` из полей `signed` (несовпадение или
  истёкший lease → пере-резервация ветвью выше; бюджета нет → POST не
  делается, backoff); после `activate` до POST — резервация `active` без
  TTL, scheduler ресендит ТОТ ЖЕ tx; POST ушёл, ответ потерян — `active`
  остаётся и разрешается кворумом статусов (v14): вердикт `confirmed` →
  `settle('spent')`.
- **Dead/redrop — ДВА вызова, строгий порядок:** (1) per-key DO атомарно
  переводит `signed → redrop_pending` под CAS (кворум dead + age guard уже
  доказаны); (2) затем `settle(старый spendKey, 'released')` в `SpendGuard`
  (на Arweave не попавшая в блок транзакция денег не стоит). `redrop_pending`
  ОБЯЗАН хранить старый `spendKey` — crash между вызовами чинится повтором
  settle. Фаза 2 берёт новый `gen` → новый `spendKey` → новый `prepare`,
  новая generation получает СВОЮ резервацию.
- **Монотонность `settle` (H1 ревью 15) — нормативно:** ретраи и cross-DO
  сбои означают, что `settle` может прийти повторно и в ЛЮБОМ порядке,
  поэтому терминальные состояния — не last-write-wins, а решётка с
  tombstone:
  - одинаковый outcome идемпотентен (повтор — no-op);
  - переход `spent → released` ЗАПРЕЩЁН (принятая сеть не «распринимается»);
  - позднее доказательство `spent` ПОСЛЕ `released` **доминирует**:
    резервация переучитывается как трата И взводится инцидентная метрика
    `spend_conflict` (released был выдан ошибочно — это сигнал о дефекте
    кворума, не штатный путь); бюджет ошибается только в безопасную сторону.
    **Бакет — час ФАКТИЧЕСКОГО settle, не исторического POST** (M ревью 16):
    запись в бакет времени POST могла бы сразу выпасть из 24-часового окна и
    вернуть занижение бюджета; писать «сейчас» — намеренно консервативно.
- **Судьба «неизвестной» резервации (ревью 12, gap) — связана с
  reconciliation, а не с таймером.** Ротация окна её НЕ снимает: пока исход
  неизвестен, резервация висит как `active` и продолжает уменьшать доступный
  остаток. Разрешение приходит от того же кворума статусов, что ведёт
  `signed`/`redrop_pending` (§ выше): вердикт `confirmed` →
  `settle('spent')` в ТЕКУЩЕЕ окно; доказанный `dead` (+ age guard) —
  двухвызовный порядок выше. **TTL для `active` намеренно НЕ вводится** — он
  освобождал бы деньги, которые могли быть потрачены. **Границы удержания
  (исправлено v17, MEDIUM ревью по v16):**
  `MAX_RECOVERY_INFLIGHT × MAX_TX_REWARD_WINSTON` ограничивает удержание
  **ОДНОГО ключа** (cap живёт в per-key DO — «bounded backlog» выше); для N
  ключей эта величина умножается на N и глобальной границей НЕ является.
  Фактическую глобальную границу задают сами пределы `SpendGuard`:
  `pendingReservations` входит в проверки `prepare`, поэтому совокупное
  удержание всех ключей не может превысить `SPEND_WINDOW_CAP_WINSTON` и
  запас над `WALLET_FLOOR_WINSTON`. **Осознанное следствие — fail-closed
  outage:** раз `active` без TTL, множество ключей с неизвестным исходом
  может бессрочно занять весь общий бюджет — новые `prepare` ЛЮБОГО ключа
  получают `503`, пока reconciliation (кворум статусов) не разрешит исходы.
  Это принятая безопасная сторона (деньги не утекают, публикации
  останавливаются), она видна по метрикам и снимается оператором/кворумом,
  а не таймером. Отдельная КОЛИЧЕСТВЕННАЯ глобальная граница числа зависших
  резерваций, если когда-либо понадобится, живёт в `SpendGuard` (глобальном
  DO), а не в per-key счётчиках.
- **Детерминированный скользящий бюджет (v16):**
  `SPEND_WINDOW_MS = 86_400_000`; часовые бакеты `spend:{hourEpoch}` в
  Winston; `rollingSpend` = сумма бакетов, покрывающих последние полные
  24 ч. **Хранится до 25 бакетов: граничный бакет удерживается, пока не
  истекли ПОЛНЫЕ 24 часа с его конца** — иначе трата исчезала бы из окна
  через 23 ч 01 мин (M3 ревью 14). Prune — только бакетов старше
  `now − 24h` целиком; тест двойной траты у границы окна обязателен. Учёт —
  в момент `settle('spent')`; `prepared`/`active` живут в
  `pendingReservations` и вычитаются отдельно.
- **Численные дефолты (ВСЕ — ПРЕДВАРИТЕЛЬНЫЕ, M5 ревью 14):**
  `BALANCE_CACHE_TTL_MS` / `PRICE_CACHE_TTL_MS` (дефолт 60 000),
  `PRICE_DEVIATION_MAX` (дефолт 1.25). События PR-2 не несут
  reward/balance/confirmations в объёме, достаточном для калибровки, поэтому
  финальные значения утверждаются по shadow-метрикам PR-3a/PR-4; что PR-2
  всё же даёт (quotedWinston, confirmations) — §4.PR-2 «Реализация».
  Источники balance/price — список `STATUS_GATEWAYS`, дедуп по canonical
  origin, перечислены явно.
- **Доверие к балансу и цене — РАЗНЫЕ правила (переписано в v15, ревью 12
  H2: медиана по ДВУМ ответам не даёт византийской стойкости — при n=2 она
  равна среднему, и один лжец сдвигает её на половину расхождения; пример
  ревью: честный `1`, лживый `100` → «медиана» 50.5 пропускает трату,
  пробивающую floor).**
  - **Баланс — консервативный МИНИМУМ валидных ответов**, требуется ≥2
    ответивших; ни одного/протух кэш → fail-closed `503`. Асимметрия
    осознана: занижающий шлюз останавливает публикации (uploads отвечают
    `503`, заметка не теряется — она и так переживает отказ загрузки), тогда
    как завышающий пробивает floor и уносит реальные деньги. Цена решения —
    один лгущий-вниз шлюз способен приостановить запись; это принято, видно
    по метрике и снимается оператором.
  - **Цена — медиана по ≥3 ответившим**; если ответили только 2 — требуется
    их ТОЧНОЕ совпадение, расхождение → `503`. Цена детерминирована
    состоянием сети, поэтому честные шлюзы совпадают цифра в цифру (живая
    проба 2026-08-20: `/price/1024` = `2961970610` на arweave.net,
    vilenarios.com и ar-io.dev — идентично), а «примерное согласие» здесь не
    нужно и вредно. Медиана по ≥3 опирается на допущение честного
    большинства источников — оно фиксируется явно.
  - Сверх того котировка выше `MAX_TX_REWARD_WINSTON` → отказ (`503`), НЕ
    оплата; отклонение от согласованного значения дальше множителя
    `PRICE_DEVIATION_MAX` (дефолт 1.25, предварительный) → отказ + метрика.
  - Короткий TTL-кэш для обоих значений (`BALANCE_CACHE_TTL_MS` /
    `PRICE_CACHE_TTL_MS`, дефолт 60 000, предварительные); истёк и кворум не
    собрался → `503`.
- **Недоступность `SpendGuard` → `503`** (как и остальные DO-зависимости).
- **Значения трёх лимитов и порядок их изменения (ревью 12, gap).** Это
  операторские величины, а не константы кода: живут как Worker vars рядом с
  `RATE_LIMIT_PER_HOUR` (`wrangler.toml`, production И staging), меняются
  деплоем и фиксируются в `docs/ROLLBACK.md` — «на горячую» без записи не
  меняются. Метод вывода (числа считает оператор перед PR-3b по свежей
  котировке): `MAX_TX_REWARD_WINSTON` = цена публикации при
  `MAX_BODY_BYTES = 51200` × запас (несколько×) — потолок ОДНОЙ транзакции,
  а не средняя цена; `SPEND_WINDOW_CAP_WINSTON` = ожидаемый суточный объём
  публикаций × типичная цена × коэффициент запаса; `WALLET_FLOOR_WINSTON` =
  запас на N суток типичного объёма, чтобы срабатывание предохранителя
  оставляло оператору время на пополнение. Опорная точка на 2026-08-20
  (живая проба): `/price/1024` = `2961970610` Winston (≈0.00296 AR за KiB) —
  одинаково на трёх шлюзах.
- **Доставка аларма — честная граница:** автоматическая защита — это САМ
  предохранитель, он не зависит от того, дошло ли оповещение. Аларм — метрика
  скорости сжигания и запаса до floor в Analytics Engine плюс строка в
  белом списке отчётов `/admin/metrics` (D4); собственный канал доставки
  (почта/вебхук) в Worker НЕ заводится — это операторский runbook в
  `docs/ROLLBACK.md`, а не код.

**Rollback floor (ревью 2, H3) — reader-before-writer в ДВА Worker-релиза:**

Старый Worker не знает статус `signed`: `check-and-reserve` примет его как
неизвестный, `release` умеет удалять только `reserved`. Прямой откат writer на
текущий Worker потерял бы `signed` или дал дубль. Поэтому:

1. **Reader-релиз:** Worker умеет безопасно ЧИТАТЬ/резюмировать `signed` И
   `redrop_pending` (реконсиляция, resend, фаза 2 redrop — ревью 7) И несёт
   ПОЛНЫЙ scheduler-контракт (dueAt, recoveryCount, alarm-обработчик, CAS,
   verdict-таблица — ревью 5; транзакционная схема и self-healing alarm —
   ревью 6), но ещё НЕ создаёт `signed` для новых upload-ов. Совместим со
   старыми клиентами (новых обязательных полей запроса нет — resume
   полностью server-side).
2. **Writer-релиз:** начинает создавать `signed`. После него — **hard
   rollback floor** (`worker-rN`): откат ниже reader-релиза запрещён.
3. Внешний `/upload`-контракт остаётся совместим со старыми клиентами.

**Тесты PR-3b (интеграционные):**

- вся матрица `reserved/signed/posted/committed` × crash до/после каждого DO
  write и до/после POST-диспатча;
- POST отправлен, ответ потерян → recheck НЕ создаёт новый txId и НЕ делает
  release;
- `5xx` от первого шлюза, тот же tx принят вторым → один txId на chain; `208` → accepted;
- истёкший anchor → пересоздание только после кворума dead;
- откат writer→reader-floor и старый клиент→новый Worker;
- невозможность resend → fail closed ДО POST, ноль вторых транзакций;
- **`signed`/`redrop_pending` переживают quota-window reset** (счётчики окна
  обнулились — записи и `recoveryCount` целы);
- **cap:** `MAX_RECOVERY_INFLIGHT` заброшенных recovery-записей → следующий upload
  получает `503` ДО подписания; после reconciliation слоты освобождаются;
- **порча `signedTx`** (байты не парсятся / не сходятся с txId) → fail
  closed: ни resend мусора, ни новой подписи, ни авто-release; выход только
  через кворум (alive→posted / dead+age→redrop);
- **DO alarm**: в состоянии `signed` — повторяет ТОЛЬКО тот же txId и
  НИКОГДА не подписывает (поправка v10, ревью 8: формулировка v9 «никогда»
  противоречила scheduler-driven фазе 2); **вызов signer разрешён только в
  фазе 2 `redrop_pending`; durable-коммит и POST разрешены только
  CAS-победителю** (поправка v12, ревью 10: «подписывает только победитель»
  из v11 противоречило подписи-до-CAS — подписать могут и проигравшие, их
  подписи эфемерны и отбрасываются);
  худший размер `signedTx` при `MAX_BODY_BYTES=51200` ≤ внутреннего предела
  ~75 KB;
- **scheduler (ревью 5):** две и более signed-записи с разными `dueAt` —
  обе реконсилируются; продвижение одной НЕ снимает alarm остальных
  (alarm исчезает только с последней recovery-записью); искусственный crash
  между записью `note:*`, `recoveryCount` и `setAlarm` → «всё или ничего»;
  alarm и обычный recheck конкурируют за один txId → ровно один переход
  (CAS); CAS отклоняет stale-результат alarm-прогона после
  `posted`/`committed`/redrop; отдельные тесты исходов `pending`,
  `unavailable`, `dead` ДО age guard (все → reschedule) и `dead` ПОСЛЕ age
  guard (→ redrop); due-записей больше `ALARM_BATCH` → остаток получает
  немедленный re-alarm;
- **атомарность/отказоустойчивость (ревью 6):** искусственный crash после
  КАЖДОГО storage-вызова внутри транзакции → полный rollback;
  статический/ревью-гейт: `blockConcurrencyWhile` нигде не используется как
  замена транзакции; исключение на ПЕРВОЙ записи batch-а не мешает обработке
  остальных и перепланированию; серия ошибок длиннее шести platform retry —
  будущий alarm сохраняется (finally + self-healing на входе запросов);
  ошибка финального `setAlarm` → backlog не сирота (ретрай + восстановление
  при следующем DO-запросе); двухфазный redrop НЕ держит storage transaction
  во время подписи или сетевых вызовов;
- **`redrop_pending` (ревью 7):** crash сразу после `signed → redrop_pending`
  (до подписи) → запись scheduler-visible, alarm стоит, фаза 2 выполняется
  на следующем прогоне; ПОСЛЕДНЯЯ запись DO в `redrop_pending` НЕ сносит
  alarm; ошибки фазы 2 дольше шести platform retry без входящих запросов →
  reconciliation продолжается (self-healing по `recoveryCount`);
  `redrop_pending` НЕ освобождает cap — upload сверх
  `MAX_RECOVERY_INFLIGHT` получает `503`; конкурентные попытки фазы 2 →
  сохраняется и отправляется только CAS-победитель; POST нового tx
  невозможен до durable-коммита нового `signed`;
- **источник данных фазы 2 (ревью 8):** alarm-driven подпись происходит
  ИСКЛЮЧИТЕЛЬНО из `redrop_pending` (негативный тест: из `signed` — никогда);
  crash-resume фазы 2 собирает новую tx из ОДНИХ durable-данных записи
  (сохранённый `signedTx`) без участия клиента — data и tags новой tx
  байт-в-байт совпадают со старой; сохранённый `signedTx` с txId ≠
  `deadTxId` или непарсящийся → fail closed, автономной подписи нет,
  клиентский `/upload` с телом реконсилирует;
- **durable/postable инвариант (ревью 9):** два конкурентных обработчика
  подписали РАЗНЫЕ tx → durable-коммитится и POST-ится только
  CAS-победитель, подпись проигравшего никуда не уходит; crash после
  подписи до CAS → следующая попытка законно подписывает повторно, но на
  POST попадает ровно ОДИН txId; `resign_violation` НЕ срабатывает на
  отброшенную эфемерную подпись и СРАБАТЫВАЕТ на второй durable/postable
  txId без нового доказанного dead;
- **откат writer→reader-floor при НЕСКОЛЬКИХ запланированных signed** —
  reader продолжает scheduler-обработку всех;
- **D10 (security):** баланс ниже floor → публикации нет (`503`) ДО подписания;
  превышен общий бюджет трат → `503` независимо от того, что per-key квота
  свободна; K ключей на своих лимитах упираются в общий потолок;
  **массовый ложный `dead` (все status-шлюзы согласованно `404` — сценарий
  общего бага монокультуры) не приводит к неограниченной серии автономных
  redrop: предохранитель срабатывает и аларм сжигания взводится**;
- **D10 tenant и повтор (ревью 12):** ДВА пользователя с ОДИНАКОВЫМИ
  `noteId/gen` получают РАЗНЫЕ резервации — попадания в чужую и «бесплатной»
  публикации не происходит; повтор `prepare` с тем же ключом и ДРУГИМ
  `reward` до подписи → атомарная пере-резервация, после создания `signed` →
  fail-closed `503`; расхождение tenant при совпавших `noteId/gen` →
  fail-closed;
- **D10 источники цифр (ревью 12):** один честный + один ЗАВЫШАЮЩИЙ баланс →
  берётся минимум, трата, пробивающая floor, не проходит; один честный + один
  ЗАНИЖАЮЩИЙ → сервис останавливается (`503`) и это ожидаемое поведение с
  метрикой; два ответа по цене с расхождением → `503` без оплаты; три
  ответа → медиана;
- **D10 неизвестный POST (ревью 12, порядок v16):** резервация переживает
  ротацию окна; последующий `confirmed` списывает её в текущее окно;
  доказанный `dead` + age guard → двухвызовный порядок: сначала CAS-переход
  `signed → redrop_pending` в per-key DO (с сохранением старого `spendKey`),
  затем `settle(старый spendKey, 'released')`; удерживаемая сумма ОДНОГО
  ключа ограничена `MAX_RECOVERY_INFLIGHT × MAX_TX_REWARD_WINSTON`
  (граница per-key, v17);
- **D10 глобальное заполнение (v17, MEDIUM ревью по v16) —
  многопользовательский тест:** K РАЗНЫХ ключей с зависшими `active`
  (неизвестный исход POST) суммарно доходят до `SPEND_WINDOW_CAP_WINSTON` /
  floor-запаса → `prepare` ЛЮБОГО нового ключа получает `503` (fail-closed
  outage), хотя per-key квоты всех свободны; reconciliation разрешает часть
  исходов (`settle`) → `prepare` снова проходит; ротация окна сама по себе
  зависшие `active` НЕ освобождает;
- **D10 сага (v16, ревью 13/15/16):** обе перестановки settle —
  (`spent`,`released`) и (`released`,`spent`): одинаковый outcome — no-op,
  `spent → released` запрещён, поздний `spent` после `released` доминирует,
  переучитывает трату И взводит `spend_conflict`; для позднего `spent` —
  проверка, что сумма легла в ТЕКУЩИЙ часовой бакет (не в час POST); crash
  между `redrop_pending`-переходом и `settle('released')` чинится повтором
  settle из сохранённого `spendKey`; истёкший lease `prepared` освобождает
  резервацию, у `active` lease/TTL нет; crash после `signed` до `activate` →
  идемпотентный `activate(spendKey, reward, revision)` повторяется
  scheduler-ом, при истёкшем lease POST не делается (backoff);
- **D10 привязка activate (v17, HIGH ревью по v16) — тест гонки
  обязателен:** `prepare` на 100 (revision r₁) → конкурентная
  пере-резервация того же `spendKey` на 1 (revision r₂) → durable `signed`
  с `{reward: 100, spendRevision: r₁}` → `activate(spendKey, 100, r₁)` НЕ
  активирует резервацию на 1: CAS ловит несовпадение, происходит атомарная
  пере-резервация на 100 под всеми проверками `prepare` (+
  `activate_remap`), `pendingReservations` учитывает 100, а не 1; вариант с
  нехваткой бюджета на 100 → `activate` отказывает, POST не делается
  (backoff), учёт НЕ занижен; идемпотентный повтор `activate` с теми же
  `{reward, revision}` — no-op;
- **D10 окно (v16, M3 ревью 14):** двойная трата у границы окна — трата,
  сделанная 23 ч 59 мин назад, всё ещё занимает бюджет (25-й бакет
  удерживается до истечения ПОЛНЫХ 24 ч с его конца); prune не трогает
  граничный бакет;
- **D10 денежные грани (ревью 11):** транзакция, которая пробивает floor
  СОБСТВЕННОЙ стоимостью, отвергается (проверка вычитает резервируемый
  reward); котировка выше `MAX_TX_REWARD_WINSTON` или далеко от медианы →
  `503` без оплаты; **конкурентные `reserve` разных ключей не пробивают
  лимит совокупно** (сериализация в одной транзакции DO); **failover-resend
  того же txId не списывает бюджет дважды** (идемпотентность по
  `{noteId, gen}`); потерянный ответ POST оставляет резервацию (учитывается
  как трата), а не освобождает её; `SpendGuard` недоступен → `503`
  (поправка v16: прежний тест «занижающий баланс не останавливает сервис
  (медиана)» противоречил правилу v15 «баланс — минимум» и удалён — верное
  ожидание задаёт «D10 источники цифр» выше: занижающий шлюз останавливает
  сервис, и это принятая цена);
- reservation/recovery/quota-инварианты не регрессируют.

### PR-4 «Multi-index restore union»

Строится поверх sentinel-модели (§1). Компоновка union и sentinel — самая
тонкая часть; ниже специфицирована явно.

- **Логическая топология (ревью 4 M1 — нормативно):** единица sweep-а —
  ЛОГИЧЕСКИЙ источник из `INDEX_SOURCES` (§2.1), не URL. Внутри источника —
  transport-fallback: страницы читаются с primary; при отказе primary (в
  пределах backoff-бюджета) источник ПЕРЕЗАПУСКАЕТСЯ с первой страницы на
  fallback-URL (курсор `after` opaque и непереносим между transport!),
  прочитанные с primary кандидаты этого источника отбрасываются — источник
  либо дочитан целиком через ОДИН transport, либо неполон. Логические
  источники — параллельно.
- `fetchPage` параметризуется URL-ом; пагинация ВНУТРИ transport
  последовательна.
- Честная оговорка (в ADR): «источник дочитан через fallback» = дочитан
  ВЫБРАННЫЙ transport; отставание fallback-транспорта от primary — то же
  остаточное умалчивание §1, новых гарантий полноты fallback не даёт.
- **Схема `block` (по ревью, high):** GraphQL `block` сейчас не запрашивается
  и по контракту nullable (`block: {height} | null`). PR-4 добавляет `block`
  в запрос и определяет runtime-схему + политику null-height (немайненные /
  ещё-не-проиндексированные): такие кандидаты упорядочиваются
  детерминированно (напр. после всех height-known, tie-break по txId ASC).
- **Union по txId ДО sentinel-логики.** Две оптимизации sentinel (drop
  «дубликата ниже известного» и claim по позиции) опираются на единый
  HEIGHT_DESC-поток одного индекса. При слиянии индексов такого потока нет.
  Поэтому: собрать union всех кандидатов → пересортировать по подтверждённому
  height (null-политика выше) → и ТОЛЬКО ЗАТЕМ применять sentinel-drop и claim.
  Иначе порядок ответов шлюзов сменит победителя цепочки (нарушение
  инвариантов тестов 9а/9г).
- **Конфликт metadata одного txId — ПЕРЕПИСАНО в v15 (ревью 12 M1).**
  Правило v6 («конфликтующий txId отбрасывается из union и помечает
  `incomplete=true`») сочинялось, когда теги индекса были ЕДИНСТВЕННЫМ
  источником атрибуции. После D9 это регрессия: отбрасывание кандидата по
  расхождению НЕПОДПИСАННЫХ данных возвращает single-index veto — один
  лживый индекс, вернув настоящий txId с поддельными тегами, заставлял бы
  клиент выбросить восстановимую транзакцию, хотя честный индекс её отдал, а
  проверенный header доступен. Нормативно теперь:
  - **union НИКОГДА не удаляет txId** — кандидат остаётся и проходит
    verify+decrypt;
  - расхождение в полях, ПОКРЫТЫХ подписью (`Note-Id`, `App-Version`, теги,
    owner, `data_root`, `data_size`), разрешается **проверенным header D9** —
    он и есть арбитр; расхождение самих индексов даёт метрику
    `index_metadata_conflict` (и `incomplete` по тому же правилу давности,
    что в D11), но НЕ удаление;
  - расхождение в НЕподписанной `block.height` не может решаться подписью:
    оно не удаляет кандидата, а разрешается детерминированной сортировкой
    (правило null-height ниже), при необходимости — высотой из кворума
    статусов; худший исход — иной порядок кандидатов, который всё равно
    не меняет победителя цепочки (победитель — по аутентифицированному
    timestamp внутри конверта, §1).
  - **Зависимость по порядку:** это правило опирается на то, что D9 уже
    выпущен (PR-3a идёт раньше PR-4 — §6). Если по какой-то причине PR-4
    пойдёт раньше D9, действует старое правило v6 (отбрасывание), потому что
    арбитра не существует.
- `opts.known` (sentinel) работает по txId → компонуется с union без изменений
  в карте: та же `KnownTxRecord`-проверка (Note-Id тег + класс версии).
- `incomplete`-семантика строгая, но по ЛОГИЧЕСКИМ источникам (ревью 4 M1):
  `incomplete=false` ⟺ каждый источник из `INDEX_SOURCES` дочитан через
  какой-либо СВОЙ transport. Отказ fallback-URL при дочитанном primary (и
  наоборот) sweep НЕ портит — fallback не может снижать доступность. Держит
  продвижение `sweep-full-at` (§1) честным.
- Правило metadata-конфликта выше применяется к расхождению между ЛЮБЫМИ
  двумя endpoint, вернувшими один txId (в т.ч. внутри одной группы — там
  расхождение теоретически невозможно и тем подозрительнее).
- **Расхождение о НАЛИЧИИ txId (D11, v13, security-ревизия F3/R3) — отдельное
  от metadata-конфликта правило:** union берётся всегда (кандидат не теряется);
  факт «txId есть у источника A и отсутствует у B» даёт метрику
  `index_presence_disagreement`; `incomplete=true` ставится ТОЛЬКО если такой
  txId **доказуемо** старше порога, при котором отставание индекса его уже не
  объясняет. **Уточнение v16: отсутствие утверждается ТОЛЬКО между
  источниками с `complete = true`** — источник, упавший посередине sweep-а,
  не дочитан и НЕ является свидетельством умалчивания (ни метрики «B
  умолчал», ни тем более флага). Так тихое умалчивание становится
  наблюдаемым, а рутинный лаг и честный отказ transport не превращают
  `incomplete` в постоянно взведённый флаг и не блокируют `sweep-full-at`.
- **Источник доказуемого возраста (v14, ревью 11 M1) — нормативно: кворум
  СТАТУСОВ, а не индекс.** Возраст берётся из `/tx/<id>/status`
  (`block_height` + `number_of_confirmations`) и считается установленным,
  когда **≥2 независимых status-origin согласны**, что транзакция
  подтверждена. `block.height`, который PR-4 добавляет в GraphQL, —
  недоверенная metadata того самого источника, чьё поведение мы и оцениваем,
  поэтому она НИКОГДА не участвует в решении о флаге — ни «за», ни «против»
  (у неё остаётся ровно одна роль: упорядочивание кандидатов, §PR-4 выше).
  **Поправка v15 (ревью 12 M2): `block = null` ПОЛНОСТЬЮ исключён из решения
  D11.** Формулировка v14 («`block = null` ⇒ свежая ⇒ флага нет никогда»)
  противоречила собственному же правилу: она возвращала индексу право
  переопределить кворум статусов — старый txId, который индекс отдал с
  `block: null`, навсегда терял бы сигнал, даже если статусы подтверждают
  сотни подтверждений. Правильная формулировка: **решение принимает ТОЛЬКО
  кворум статусов; что вернул индекс в `block` — не имеет значения.**
  Нормативные следствия:
  - **порог задаётся в ПОДТВЕРЖДЕНИЯХ, а не в миллисекундах:**
    `MIN_PRESENCE_CONFIRMATIONS`. Причина: `/tx/<id>/status` возвращает
    `block_height` и `number_of_confirmations`, а не отметку времени;
    переводить их в миллисекунды значило бы вводить оценку времени блока в
    доверенный путь. `MIN_COMMITTED_AGE_MS` (30 мин) остаётся тем, чем и был
    — guard-ом Worker-а перед redrop; это ДРУГИЕ часы, смешивать их нельзя.
    Значение калибруется в PR-4 по данным PR-2 (ориентир: типичный интервал
    блока Arweave ~2 мин ⇒ порядок десятков подтверждений заведомо
    перекрывает любой честный лаг индекса);
  - **нормативные пороги (v16; единственная таблица D11; ВСЕ значения —
    ПРЕДВАРИТЕЛЬНЫЕ до shadow-метрик PR-3a/PR-4, M5 ревью 14):**

    | Константа | Дефолт | Роль |
    |---|---|---|
    | `MIN_PRESENCE_CONFIRMATIONS` | 50 | порог давности в подтверждениях |
    | `MAX_STATUS_HEIGHT_SKEW` | 5 блоков | допуск расхождения `block_height` между origin (разные вершины цепи) |
    | `MAX_PRESENCE_PROBES_PER_SWEEP` | 20 | потолок проверяемых расхождений на прогон |
    | дедлайн проверок | общий со sweep | отдельного таймера нет |

  - **правило согласия и независимость (v16, M4 ревью 14 — fail-closed):**
    возраст установлен, когда **≥2 НЕЗАВИСИМЫХ status-источника** сообщают
    транзакцию подтверждённой и у обоих `number_of_confirmations ≥
    MIN_PRESENCE_CONFIRMATIONS`. **Независимость = РАЗНЫЕ операторы:** в
    конфиг шлюзов добавляется пиненная карта **`canonicalOrigin →
    operatorId`** (build-time, как сами списки); голоса кворума
    **дедуплицируются по `operatorId`** — два origin одного оператора дают
    ОДИН голос. Config-checker (PR-6) падает при дублях origin, при origin
    без известного `operatorId` и при расхождении карты клиент/Worker. У
    пяти утверждённых шлюзов операторы разные (§2.1) — правило работает уже
    сегодня. Расхождение `block_height` между origin в пределах
    `MAX_STATUS_HEIGHT_SKEW` допустимо; расхождение о САМОМ факте включения
    или о блоке за пределами допуска ⇒ возраст НЕ установлен;
  - кворум подтвердил возраст ⇒ `incomplete=true` **независимо от того, что
    сказал GraphQL** (в т.ч. при `block: null`);
  - кворум не собрался или возраст не установлен ⇒ метрика есть, флага НЕТ.
    Недоказуемый случай не имеет права поднимать пользовательский сигнал;
  - **чистое устройство** локального `firstSeenAt` не имеет — правило
    работает и там, потому что не зависит от локального состояния; там, где
    `firstSeenAt` есть, он может служить ДОПОЛНИТЕЛЬНОЙ нижней границей, но
    никогда единственным источником.
  Смысл конструкции: лживый индекс не может управлять флагом ни в одну
  сторону — подделать возраст он не может (возраст приходит от
  status-источников), скрыть расхождение тоже не может (union его уже увидел
  и метрика взведена), и «занулить» возраст через `block: null` больше не
  может.
- **Бюджет проверок возраста (ревью 12, gap; уточнено v16):** каждое
  расхождение стоит до пяти status-запросов, а расхождений в плохом сценарии
  много. Нормативно: результаты статусов кэшируются в пределах прогона;
  проверки идут в общей ограниченной параллельности и под ОБЩИМ дедлайном
  sweep-а; на прогон действует потолок `MAX_PRESENCE_PROBES_PER_SWEEP`
  (таблица выше). Выбор кандидатов детерминирован: старейшие по локальному
  `firstSeenAt`, затем `txId ASC`.
  Исчерпан бюджет ⇒ для непроверенных расхождений метрика есть, флага нет —
  та же безопасная сторона, что и «возраст не доказан». Занижение флага здесь
  безвредно: сама заметка УЖЕ восстановлена union-ом, флаг лишь сигнализирует
  о недобросовестности источника.
- **Инвариант, который PR-4 НЕ имеет права ослабить (security-ревизия §4):**
  restore остаётся АДДИТИВНЫМ upsert-repair (`storage.ts:295–312`, `376–395`)
  — никакого «restore перезаписывает/удаляет более новую локальную версию».
  Именно это защищает устройство с уже имеющейся новой ревизией от подсунутой
  старой; переработка union и сортировки проходит рядом с этим кодом, поэтому
  инвариант закрепляется тестом.
- `ArweaveIndexUnavailableError` — только когда ПЕРВАЯ страница упала у ВСЕХ
  transport ВСЕХ источников (не по abort).

**Backoff (по ревью, medium):**

- per-index: число попыток + **общий бюджет времени** (deadline), обработка
  всех `5xx` и `Retry-After`, **abort-aware ожидание** — и fetch, и backoff
  timer отменяются caller-signal (lock/reset не должен ждать мёртвый secondary
  несколько таймаутов).
- Уточнение теста: «первый индекс `503`, второй полный» → при ВРЕМЕННОМ `503`
  (успех в пределах бюджета) restore полный, `incomplete=false`; при
  ПОСТОЯННОМ отказе первого в пределах бюджета — `incomplete=true`. Тест
  разделяет два случая явно.

**Регрессия / порядок кандидатов (ревью 2, M2).** Обещание «бит-в-бит»
уточняется: новая сортировка `height + txId ASC` могла бы изменить порядок
кандидатов одного блока и `block=null` относительно нынешнего GraphQL
HEIGHT_DESC, а для одинакового Note-Id порядок влияет на sentinel-drop и claim.
Решение: **в single-index режиме сохраняется исходный edge-order индекса
(HEIGHT_DESC) без пересортировки** — 12 тестов `arweave.incremental.test.ts`
(поправка v5: пересчитано, в v4 стояло «16») проходят бит-в-бит. Новая детерминированная сортировка применяется ТОЛЬКО при
union ≥2 индексов, где единого потока и так нет. Новые тесты: equal-height,
mixed null-height, перестановки ответов индексов не меняют победителя;
**primary transport упал, fallback дочитал → источник complete,
`incomplete=false`; fallback упал, primary дочитал → тоже complete;
рестарт на fallback не смешивает страницы двух transport (курсор
непереносим).** Тесты D11/security: один индекс молча опускает подлинный txId,
второй его возвращает → union восстанавливает заметку, метрика расхождения
взведена; тот же txId СТАРШЕ порога по **кворуму статусов** → дополнительно
`incomplete=true`; СВЕЖИЙ txId (лаг) → `incomplete` НЕ поднимается;
**(v16) источник упал посередине (`complete=false`) → его «молчание» не
считается расхождением: ни метрики против него, ни флага; два голоса
возраста от origin ОДНОГО `operatorId` → один голос, возраст НЕ установлен
(дедуп по оператору), третий origin другого оператора добирает кворум;
расхождение `block_height` в пределах `MAX_STATUS_HEIGHT_SKEW` не мешает
согласию, сверх допуска — возраст не установлен;**
**`block = null` ПРИ кворуме статусов с большим числом подтверждений →
`incomplete=true` (индекс не может занулить возраст); чистое устройство без
`firstSeenAt` → правило работает от кворума; кворум не установил возраст →
метрика есть, флага нет; индекс сообщает заведомо старую высоту, а статусы
говорят иное → выигрывают статусы; массовые расхождения упираются в потолок
проверок и общий дедлайн → непроверенные дают метрику без флага**;
**конфликт НЕподписанных тегов GraphQL при валидном header D9 → транзакция
ВОССТАНАВЛИВАЕТСЯ (кандидат не удаляется), арбитр — header, конфликт даёт
метрику;** оба индекса опускают →
расхождения нет, поведение как сегодня (граница §1 — умалчивание всех
источников не детектируется, см. отложенный ADR high-water mark в §3);
downgrade-сценарий: на устройстве с уже имеющейся новой ревизией она остаётся
победителем цепочки, restore ничего не перезаписывает.

### PR-5 «restore.html на Arweave»

**Critical-1 (принято): trust-модель v1 недостаточна.** txId и AES-GCM
защищают ДАННЫЕ заметок, но не делают HTML-ответ шлюза доверенным исполняемым
кодом. Нечестный шлюз может отдать изменённую страницу, переписать её CSP и
украсть введённую seed ДО любой проверки envelope. SHA-256-сверка после
публикации проверяет релиз в момент публикации, а не байты, которые браузер
получит позже. Дополнительно: запрет собственных localStorage/IndexedDB/SW НЕ
защищает от ранее установленного ЧУЖОГО Service Worker на общем origin шлюза
(worker в корне получает корневой scope). ar.io для этого вводит
tx-specific sandbox subdomains.

**Trust-модель — ВЫБРАН вариант B (реш. 1 §7): скачиваемый файл.** HTML
распространяется как файл с независимой проверкой SHA-256, НЕ как исполняемая
страница по gateway-URL.

**Механики file-mode (ревью 2 H1, ревью 3) — нормативно, без альтернатив:**

Ключевой вывод ревью 3: MIME, `Content-Disposition` и расширение контролирует
ШЛЮЗ — недоверенный шлюз может исполнить подменённый HTML при прямой
браузерной навигации ДО проверки SHA-256. Значит барьер НЕ должен зависеть от
кооперации шлюза и НЕ может быть «инструкцией пользователю». Нормативно:

- **Доверенный путь = проверка в уже доверенном коде (основной).** Байты
  restore получает и проверяет НЕ переход по gateway-URL, а **доверенный
  агент**: (а) само PWA (это уже доверенный код с запиненным эталонным
  SHA-256) — фонить байты restore с ≥1 шлюза, сверить хеш в приложении и
  отдать пользователю как проверенный Blob для сохранения/открытия; либо
  (б) для холодного восстановления, когда приложения нет, — **отдельная
  неисполняющая проверочная утилита** (downloader/verifier), которая
  скачивает файл, считает SHA-256 и лишь при совпадении отдаёт его.
  Прямой клик по gateway-URL НЕ является поддерживаемым способом запуска.
- **Verifier — только ПК (решение владельца 2026-08-19); полный cold-flow
  нормативен (ревью 4 M2).** Основной путь — проверка внутри PWA (пункт (а)
  выше; PWA качает **строго `/raw/<txId>`**, НЕ корневой `/{txId}` с
  sandbox-redirect). Холодный ПК-путь — ЦЕЛИКОМ терминальный, браузерной
  навигации на gateway-URL нет ни на одном шаге:
  1. `curl --fail --location --output restore.download
     "https://<gateway>/raw/<txId>"` (curl штатен в Windows 10+/macOS/Linux);
  2. **программная сверка + rename ОДНИМ копируемым блоком (ревью 5 M2):**
     человек НЕ сравнивает hex-строки глазами — сравнение и переименование
     выполняет команда, rename недостижим без совпадения. Windows
     (PowerShell):

     ```powershell
     $E='<ЭТАЛОННЫЙ_SHA256>'
     if ((Get-FileHash restore.download -Algorithm SHA256).Hash -eq $E) {
       Rename-Item restore.download restore.html; 'OK: открывайте restore.html'
     } else { Remove-Item restore.download; Write-Error 'HASH MISMATCH' }
     ```

     macOS/Linux (sh):

     ```sh
     E='<ЭТАЛОННЫЙ_SHA256>'
     echo "$E  restore.download" | shasum -a 256 -c - \
       && mv restore.download restore.html \
       || { rm -f restore.download; echo 'HASH MISMATCH' >&2; }
     ```

  3. открыть `restore.html` локально (file://). При mismatch файл УДАЛЁН
     (а до rename носит неисполняемое расширение `.download`) — «открыть
     несмотря на mismatch» требует сознательных ручных действий вне
     инструкции.
  Блоки генерируются скриптом публикации релиза с УЖЕ подставленным эталоном
  (не заполняются руками) и живут в доверенных местах ниже. Отдельный
  скачиваемый verifier-артефакт НЕ вводится — он воспроизвёл бы ту же
  проблему доверия к своим байтам; используются ТОЛЬКО штатные системные
  утилиты. **Канал доверия к инструкции и эталонному хешу:** README в
  GitHub-репозитории, экран настроек приложения и `docs/ROLLBACK.md` — те же
  доверенные места, где живёт txId; шлюз не участвует. **Мобильное холодное
  восстановление БЕЗ приложения объявляется неподдерживаемым** (фиксируется
  в ADR и README); отдельный мобильный проверочный инструмент — отложенная
  задача в бэклог, НЕ в PR-5. Hash mismatch ОБЯЗАН блокировать (тест);
  полный terminal-only сценарий download → hash → open — в acceptance.
- **Заголовки шлюза — вспомогательны, не барьер.** Публиковать с
  `application/octet-stream` / `Content-Disposition: attachment` полезно, но
  это НЕ считается защитой (шлюз волен игнорировать). Защита — только
  проверка хеша доверенным агентом выше.
- **file:// = opaque origin `null` + CORS.** Открытый локально файл шлёт
  запросы к шлюзам с `Origin: null`. GraphQL и `/raw` каждого D7-шлюза ОБЯЗАНЫ
  отвечать `Access-Control-Allow-Origin: *` (или допускать `null`). Проверяется
  из реального `file:///` контекста на поддерживаемых браузерах (acceptance).
- **Acceptance:** «gateway-код НЕ исполняется до checksum verification»
  (заменяет неприменимый к file-mode критерий про SW/sandbox).

**Прочее (по ревью, medium):**

- рендер только `textContent`/безопасные DOM API (никакого `innerHTML`);
- явный JSON-export по действию пользователя, без автосохранения формы и
  неявного экспорта; поля seed `autocomplete="off"`, `spellcheck="false"`;
- очистка ссылок на `pagehide` (без обещания физической zeroization);
- форматы v1–v4, gateway-пул D1 + ручное добавление, multi-index sweep из
  PR-4 (общий код вынесен без DOM-зависимостей);
- CSP через `<meta>`: `default-src 'none'`, инлайн-хеши, `connect-src` —
  осознанно шире PWA (иначе ручной шлюз невозможен), trade-off в ADR.

**Публикация и ротация (по ревью, high):**

- публикация ОТДЕЛЬНЫМ release-кошельком и подписанным release-manifest —
  НЕ production upload wallet (не расширять границу секрета на локальную
  машину); скрипт скачивает файл с ≥2 шлюзов и сверяет SHA-256 со сборкой.
- **Хранение release-кошелька (решение владельца 2026-08-19):** генерация
  оффлайн на этапе PR-5; основная копия — менеджер паролей, запасная —
  бумажная в другом месте; НИКОГДА в repo/CI/облачной синхронизации в
  открытом виде и НИКОГДА в самих Eternal Notes (циклическая зависимость);
  JWK касается диска только на время релиза; баланс — минимальный (публикация
  стоит центы).
- **Immutable restore пинит owners/gateway/форматы навсегда** → при wallet
  rotation порядок ОБЯЗАТЕЛЕН: (1) публикация нового restore с old+new
  owners → (2) обновление ссылок/verifier → (3) клиент → (4) переключение
  Worker JWK. Иначе старый restore не увидит новые записи. То же для новой
  App-Version (напр. v5): restore должен знать формат до появления таких
  записей on-chain.
- txId нового restore: README, экран настроек (`ar://<txId>`),
  `docs/ROLLBACK.md`.

**Acceptance:** восстановление при полном отключении Matamata/Cloudflare/Solana;
gateway-код НЕ исполняется до checksum verification (file-mode); **полный
terminal-only cold-flow: curl-download → ПРОГРАММНАЯ сверка → rename/open
только через `&&`/`if`** (ревью 4 M2 + ревью 5 M2); **hash mismatch
программно запрещает rename/open — автотест прогоняет оба командных блока
(PowerShell и sh) на подменённом файле и убеждается, что `restore.html` не
возник, а `.download` удалён**; GraphQL и
`/raw` работают из `file:///` `Origin: null` на поддерживаемых браузерах —
**с per-gateway фиксацией `Access-Control-Allow-Origin` на 200-ответах
каждого `PAYLOAD_GATEWAYS`** (ревью 4 M3; предварительные замеры — §2.1);
malicious HTML/script/formula payload безопасно рендерится (`textContent`);
BFCache-lifecycle; форматы v1–v4.

### PR-6 «CI / deploy / rollback wiring» (по ревью, high — пронизывает все PR)

Не отдельная фича, а обязательная часть каждого PR-3a/3b/4:

- новые Vite env и Worker vars/bindings добавлены в: GitHub Actions,
  GitHub Pages, Cloudflare Pages, Worker **production И staging**, staging
  config checker;
- **production build fail-closed**: требует утверждённый multi-gateway набор,
  в т.ч. **`MIN_STATUS_ORIGINS = 2`** (бывш. H4; в v6 это ЕДИНСТВЕННЫЙ порог —
  runtime-порога «ответивших» больше нет, §4.PR-3a), — иначе прод незаметно
  останется на одном `arweave.net` или dead будет недостижим;
- **идентичность списков клиент/Worker (ревью 5):** формула dead одна на
  двоих, значит `STATUS_GATEWAYS` клиента и Worker ОБЯЗАНЫ совпадать —
  deploy/staging config checker сверяет нормализованные (канонизация+дедуп
  PR-3a) списки из Vite env и Worker vars и падает при расхождении;
- **карта `canonicalOrigin → operatorId` (v16, M4 ревью 14):** пиненная
  build-time карта операторов для кворума D11 — config-checker падает при
  дублях origin, при origin без известного `operatorId` и при расхождении
  карты клиент/Worker (fail-closed: неизвестный оператор не получает голос
  молча);
- **Worker rollback floor (H3):** PR-3b выкатывается reader-релизом ПЕРЕД
  writer-релизом; после writer фиксируется hard floor `worker-rN` в
  `docs/ROLLBACK.md`. DO-схема `signed` де-факто требует reader-before-writer
  миграции — «каждый PR независимо откатываем» для write-path НЕВЕРНО и
  заменяется явным floor;
- `docs/ROLLBACK.md` дополняется per-PR: required secrets/vars, owner-rotation
  порядок, rollback floor, критерий отката — доказывается, а не постулируется.

## 5. Метрики успеха (по данным PR-2)

- **новые DURABLE/POSTABLE txId на одну версию без подтверждённого dead →
  0** (v11: эфемерные подписи не считаются — см. §4.PR-2) (главный
  security-KPI, H4/H2); `post_attempt_same_tx` (норм. failover) и
  `redrop_new_tx` считаются РАЗДЕЛЬНО;
- доля `status_verdict=dead`, опровергнутых кворумом → видима и падает;
- p95 latency и доля `5xx/timeout` по каждому хосту → основание D7;
- **`payload_hash_mismatch` → 0 в норме; любой ненулевой всплеск по одному
  хосту — основание исключить его из `PAYLOAD_GATEWAYS`** (D9);
- **`index_presence_disagreement` со признаком «старше порога» → 0 в норме;**
  систематический рост — триггер отложенного ADR про high-water mark (§3);
- **скорость сжигания кошелька и запас до floor — видимы и алармят** (D10);
- restore c `incomplete=true` — недоступна с клиента (D5); прокси-метрику по
  серверным пробам сознательно НЕ вводим.

## 6. Порядок выполнения (утверждён владельцем 2026-08-19)

Изменение против v4: write-path (PR-3b) сдвинут ПОСЛЕ read-path целиком и
автотриггеров. Обоснование: сбой чтения бьёт по restore/sync немедленно, а
сбой загрузки сегодня не теряет заметку («попробуй позже»); PR-3b — самый
рискованный кусок (state machine DO, два последовательных Worker-релиза с
floor, платные staging-испытания) и не должен задерживать автотриггеры.

1. PR-1: ADR + этот план (актуальная ревизия — в шапке документа; PR-1
   смержен в main 2026-08-20 с v14, дальнейшие редакции катятся через
   PR #87) (D7 утверждён — §2.1; формула кворума и
   логические источники — v6; scheduler и hash-барьер — v7;
   transaction-атомарность и alarm-fault-tolerance — v8; durable
   `redrop_pending` + `recoveryCount` — v9; generation-правило подписи и
   durable-источник фазы 2 — v10).
2. Transport adapter + метрики со staging/CI (PR-2 + PR-6).
3. Read-only multi-gateway: status + validated payload fallback (PR-3a + PR-6).
   **Гейт D9:** пул payload-шлюзов выпускается ТОЛЬКО вместе с верификацией
   `txId ↔ bytes` (не влезло в бюджет бандла — остаётся одиночный шлюз).
4. Multi-index union с conflict/null-height политикой (PR-4).
5. **Автотриггеры sweep (+ дальше /feed)** — прежний roadmap-трек, вставлен
   здесь: авто-sweep сразу ходит по нескольким шлюзам/индексам, а не
   увеличивает нагрузку на единственный `arweave.net`.
6. Upload failover ТОЛЬКО с одним signed txId (PR-3b, предусловия: persist
   signed tx **и глобальный предохранитель кошелька D10 — до writer-релиза**;
   платные staging-испытания разрешены). Может идти параллельно с п.5 —
   треки не пересекаются по коду. Reader-релиз предохранителем не блокируется.
7. restore.html — file-mode (реш. 1): доверенный агент проверяет SHA-256 до
   исполнения; `Origin:null` CORS-acceptance по D7 (PR-5); release-кошелёк
   создаётся на этом шаге (§4.PR-5). Sandbox НЕ требуется для этого пути
   (реш. 6 §7 — подтверждён вживую, вне критического пути).
8. Обновить rollback/rotation runbooks; fault-injection + staging smoke.

## 7. Решения по семи вопросам ревью (утверждены владельцем 2026-08-19)

1. **restore.html — распространяется как СКАЧИВАЕМЫЙ ФАЙЛ** (вариант B §4.PR-5)
   с независимой проверкой SHA-256, НЕ как исполняемая страница по gateway-URL.
   Это снимает critical-1: браузер исполняет байты только после того, как
   пользователь сверил контрольную сумму. PR-5 разблокирован в этой модели.
2. **Upload failover — только тот же signed tx/txId** (critical-2 закрыт).
   Persist подписанной транзакции до первого POST **возвращён в объём** (§3,
   предусловие PR-3b).
3. **`dead` = строгий N-of-N** — уточнено ревью 4 (H1) до формулы:
   `deadVotes === configuredOrigins.length && configuredOrigins.length >= 2`.
   КАЖДЫЙ настроенный origin должен ответить `404`; любой timeout/`429`/`5xx`/
   неответивший делает вердикт `unavailable` (пример: `2×404 + 3×timeout` →
   `unavailable`). Недоступный источник не просто «не голосует за» — он
   блокирует dead целиком.
4. **Индексы — полные публичные GraphQL URL** (D8). Goldsky остаётся
   кандидатом; клиентский bearer не используем (только публичные endpoints).
5. **Конфликт metadata / `block=null` — решение ПЕРЕСМОТРЕНО (v15 ревью 12
   M1, зафиксировано v16).** Исходная формулировка 2026-08-19
   («конфликтующий txId помечает sweep `incomplete=true`») писалась, когда
   теги индекса были единственным источником атрибуции; после D9 она стала
   регрессией (single-index veto). Действующее правило — §4.PR-4: union
   НИКОГДА не удаляет txId; расхождение подписанных полей разрешает
   проверенный header D9 (арбитр), давая метрику `index_metadata_conflict`;
   `block=null` полностью исключён из решения D11 и лишь упорядочивается
   детерминированно в конец (tie-break txId ASC). Старое правило применимо
   ТОЛЬКО если PR-4 выйдет раньше D9 (§4.PR-4, «зависимость по порядку»).
6. **Sandbox-шлюзы — research ВЫПОЛНЕН (v5, 2026-08-19).** Подтверждено
   вживую: `permagate.io/{txId}` и сам `arweave.net/{txId}` отвечают
   `302 → https://<base32(txId)>.<gateway>/…` — каждая транзакция изолируется
   в собственный origin (чужие cookie/localStorage/SW недоступны). При этом
   `/raw/{txId}` и `/graphql` редиректу НЕ подвержены — текущий CSP и клиент
   не затронуты. Остаётся закрепить acceptance-тестом в PR-3a (редирект не
   ломает `/raw`-путь). НЕ на критическом пути restore, раз выбран
   скачиваемый файл (реш. 1).
7. **Wallet rotation — «сначала новый restore», отдельный release-кошелёк.**
   Порядок (runbook в `docs/ROLLBACK.md`): опубликовать новый restore со
   старым+новым owner отдельным release-кошельком → обновить ссылки/verifier
   → выкатить клиент → переключить `ARWEAVE_JWK` Worker-а. То же для новой
   App-Version (restore знает формат до появления таких записей on-chain).
   Боевой upload-кошелёк на локальную машину не выносится.

## 8. Карта соответствия (статус v17)

| PR | Статус | Главное, что закрыть |
|---|---|---|
| PR-1 ADR | **ready** | trust-инвариант restore (checksum-before-exec), same-tx, остаточная полнота, failure-domain оговорка (§2.1) |
| PR-2 метрики | **ready — полная реализационная спецификация в §4.PR-2 «Реализация» (v16); единственный незаблокированный кодовый шаг** | transport adapter (no hidden SDK req), 5 фактически пишущихся событий + `docs/METRICS.md` строго по написанному, отдельный `METRICS_ADMIN_SECRET`, `/admin/metrics` по контракту H2 ревью 15, dev-smoke гейт (§R6) |
| PR-3a read | **не стартует до ревью протоколов** (v15); объём вырос на гейт D9 | **верификация `txId ↔ bytes` перед пулом payload + правка комментария F1 (security)**, строгий N-of-N (H1 ревью 4), `MIN_STATUS_ORIGINS=2`, dedup по типу (H5), payload-validation fallback, acceptance «редирект не ломает /raw» |
| PR-3b write | спецификация завершена; **writer-релиз заблокирован предусловием D10 + ревью протокола** (reader — нет); ждёт своей очереди (§6 п.6) | **предохранитель кошелька в Winston: floor + бюджет + per-tx cap + идемпотентная сага `prepared/active/settled` по `spendKey = {publicKeyB64, noteId, gen}` с монотонным settle и 24-ч окном часовых бакетов (v16, ревью 13–16) + привязка `activate(spendKey, reward, revision)` CAS-ом к durable `signed` (v17) + баланс-минимум / цена-медиана (security F5/R5, протокол v14–v17)**, согласованность alarm-теста/ADR/cap с durable-инвариантом (ревью 10), durable/postable-инвариант generation + `resign_violation` по durable-txId + `deadTxId`-lineage (ревью 9), durable-источник фазы 2 из сохранённого `signedTx` (ревью 8), durable `redrop_pending` + единый `recoveryCount`-cap (H1 ревью 7), `storage.transaction`-атомарность + try/finally + self-healing alarm (ревью 6), single-alarm scheduler + CAS + verdict-таблица (ревью 5), inline `signedTx` + cap (H2 ревью 4), `signed`/`redrop_pending` не подлежат release/TTL (ревью 3), reader-floor несёт scheduler + фазу 2 (H3/ревью 7), anchor expiry, staging paid-tx испытания → утверждение `UPLOAD_GATEWAYS` |
| PR-4 union | risky; **v16: формула D11 однозначна — отсутствие только между `complete=true`, независимость по `operatorId` (fail-closed), пороги таблицей; подлежит ревью** | **D11: presence-расхождение → метрика, `incomplete` по порогу давности от кворума статусов; restore остаётся аддитивным (security F3/R3)**, логические `INDEX_SOURCES` (M1 ревью 4), single-index edge-order сохранён (M2), metadata-конфликт → арбитр header D9 + метрика (v15), nullable height, abort-backoff |
| PR-5 restore | ready (программный hash-барьер v7) | копируемые команд-блоки с `&&`/`if` (M2 ревью 5), per-gateway `/raw` CORS-фиксация (M3 ревью 4), safe render, release-кошелёк (§4.PR-5) |
| PR-6 CI/deploy | обязателен в каждом | `MIN_STATUS_ORIGINS=2`, **Worker rollback floor (H3)**, env/bindings везде |

**Спецификационных блокеров по части v12 (доступность и write-path-механика)
НЕ осталось** — единственный HIGH ревью 7 закрыт durable-состоянием
`redrop_pending` и единым `recoveryCount`-cap в v9. **Заявление НЕ
распространяется на security-часть:** v13 внесла D9–D11 как правила без
протоколов, ревью 11 справедливо квалифицировало это как два high и один
medium; протоколы дописаны в v14 (§4.PR-3a «Верификация D9», §4.PR-3b
«Предохранитель D10», источник возраста в §4.PR-4) и подлежат отдельному
раунду ревью — до него PR-3a, writer-часть PR-3b и PR-4 не стартуют.
Остальные пункты реализационные:
(1) PR-3b: transaction-scheduler по v8/v9 и reader-before-writer floor;
(2) PR-3a/4: формула кворума и логические источники по v6; (3) PR-5:
исполнение cold-flow контракта (генерация команд-блоков скриптом публикации)
+ CORS в acceptance. Открытым в D7 остаётся только `UPLOAD_GATEWAYS` —
утверждается по staging-испытаниям в PR-3b. **PR-1 и PR-2 можно начинать**;
спецификации PR-3a, PR-4 и PR-5 признаны готовыми ревью 5–6.

**Добавлено v13 (security-ревизия) — не спецификационные блокеры, а условия
выпуска:** (а) **PR-3a**: пул payload-шлюзов не выпускается без верификации
`txId ↔ bytes` (D9) — при нехватке бюджета бандла выпускается одиночный шлюз;
(б) **PR-3b**: writer-релиз не выпускается без глобального предохранителя
кошелька (D10) — reader-релиз этим не блокируется; (в) **PR-4**: presence-
расхождение индексов даёт метрику всегда и `incomplete` по порогу давности
(D11), аддитивность restore закрепляется тестом; (г) отложенный ADR про
подписанный high-water mark — §3 (единственная защита от downgrade на чистом
устройстве, вне автономного restore, без height-курсора).

Из ревью 1 закрыты 9 из 12; из ревью 2 приняты все 8; из ревью 3 — все 3;
из ревью 4 (по v5) — все 5 (2 high: формула dead-кворума, bounded lifecycle
`signed`; 3 medium: логическая топология индексов, терминальный cold-flow,
CORS-замеры `/raw`); из ревью 5 (по v6) — все 4 (high: single-alarm
scheduler + CAS; medium: verdict-таблица alarm-прогона, программный
hash-барьер; low: лимит SQLite-backed DO = 2 MB, не 128 KiB); из ревью 6
(по v7) — оба (high: `storage.transaction` — единственный механизм
атомарности, `blockConcurrencyWhile` удалён как альтернатива; medium:
try/finally + self-healing alarm-reschedule; плюс два уточнения из
вопросов: изоляция исключений по записи, двухфазный redrop); из ревью 7
(по v8) — high принят целиком (durable `redrop_pending`: scheduler-visible,
свой dueAt, в общем `recoveryCount`-cap, capacity не освобождается в фазе 1,
POST только после durable-коммита нового `signed`, backoff при ошибке
фазы 2; reader-floor возобновляет фазу 2); из ревью 8 (по v9) — оба medium
(«alarm не подписывает» ограничено `signed` — фаза 2 `redrop_pending`
единственная alarm-driven подпись; durable-источник фазы 2 — сохранённый
старый `signedTx`, сверенный с `deadTxId`, data/tags переносятся без
изменений) плюс gap-уточнение «подпись ровно один раз на generation»;
из ревью 9 (по v10) — medium принят (инвариант переформулирован: ровно один
DURABLE/POSTABLE txId на generation, POST только у CAS-победителя, эфемерные
подписи проигравших/оборванных попыток отбрасываются; `resign_violation`
считает второй durable-txId без нового dead, не вызовы signer; `deadTxId?`
закреплён в схеме `signed` как lineage); из ревью 10 (по v11) — medium + 2
gap (alarm-тест: «вызов signer — только фаза 2; durable-коммит и POST —
только CAS-победителю»; ADR same-signed-tx через durable/postable txId; cap
через recovery-записи). Вердикт ревью 10: после этих механических правок
план готов; остальные PR не заблокированы.
